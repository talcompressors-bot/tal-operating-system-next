# TASK BOARD

Last updated: 2026-07-01
Mode: CAPABILITY_BUILDING; governance frozen; TDOS frozen in Maintenance Mode; Tal Intelligence Core architecture documented as the single owning architecture for business intelligence and knowledge learning; domain-driven roadmap realigned; Autonomous Business Draft Generation Sprint 12 implemented; Production Draft Review and Learning Loop Sprint 11 implemented; Production Draft Generation Sprint 10 implemented; Business Intent Policy Layer Sprint 9 implemented; Generalized BusinessDocument Draft Gateway Sprint 8 implemented; End-to-End Service Flow MVP Sprint 7 implemented; Customer 360 Workspace Sprint 6 implemented; Operations Command Center Sprint 5 implemented; Operations Center Sprint 4 implemented; Financial Capability Sprint 3 implemented; Commercial Lifecycle Hardening Sprint 2 implemented; BusinessCase Runtime Sprint 1 implemented; Universal Business Document Engine foundation implemented; TDOS risk-based operating model integrated; Commercial, Financial, customer, operational workspace, service-flow integration, draft-gateway, intent-policy, production draft generation, approved-correction learning, and autonomous Hebrew draft generation runtime work started; Maven remains an External Adapter gate only; no real Maven execution approved yet

## NOW

| Task | Goal | Test / Done | Approval Needed |
|---|---|---|---|
| Next Tal Intelligence Core runtime capability selection | Select the next runtime capability through the Chief Architect evidence loop after Asset Intelligence runtime was implemented. Recommended candidate: strengthen operational recommendations from Asset Intelligence into Service Intelligence only if it can reuse current evidence without schema changes or protected writes. | Selected task states Company Objective, Business Decision / Capability, existing assets reused, alternatives rejected, protected systems check, validation proof, and approval gate before implementation | Explicit approval required before schema changes, DB writes/imports beyond approved protected flows, Maven/Invoice4U execution, customer-facing/email action, inventory action, source-system/cloud/production work, package install, external export, or reopening TDOS outside the approved exceptions |

## NEXT

| Task | Goal | Test / Done | Approval Needed |
|---|---|---|---|
| Correction persistence type expansion, if explicitly selected | Review whether approved draft learning needs structured persistence beyond existing BusinessDocumentItem rawSource and BusinessDocumentLog rawData for title, line add/remove/reorder, narrative, document type, pricing-policy, or rejection taxonomy corrections | Recommendation identifies what can remain in existing logs and what would require schema/new DB writes before any implementation | Explicit approval before schema change, DB write outside existing protected line-resolution/approval flows, package install, or new learning source of truth |
| BusinessDocument type expansion design, if explicitly selected | Review current enum/document configuration gaps for true Tax Invoice / Receipt, Purchase Order, Delivery Note, and Debit Note support | Approved design identifies required enum/schema changes, ownership Domains, runtime boundaries, validations, and migration strategy before implementation | Explicit approval required before schema or enum change |
| Operations Command Center queue-state persistence, if explicitly selected | Add persisted assignment, due dates/SLA, user-specific work ownership, handled-today state, and close/resolve workflow only if Liad wants the Command Center to become writable | Command Center becomes a true execution work queue with approved persistence rather than read-only projections | Explicit `SCHEMA_OR_DATA_CHANGE` approval required before schema change, DB write, queue mutation, close/resolve action, or migration |
| Inventory and Procurement boundary/readiness | Define how parts, supplier, procurement, stock, reservations, and future delivery-note work interact with Service Operations, Commercial documents, Financial status, and BusinessCase | Boundary/runtime readiness identifies owner Domain, dependencies, mutation gates, and validation without inventory deduction | Explicit approval before inventory action, procurement write, schema change, import, package install, or external action |
| FinancialEvidence persistence and attachment storage, if explicitly selected | Persist FinancialEvidence, attachment IDs/files, duplicate registry, and approved issuing workflow only after a separate approval gate | Financial Runtime remains owner; extracted data remains suggestion-only; no receipt/tax invoice is issued automatically | Explicit approval before schema change, DB write, storage/privacy/retention implementation, OCR, bank API, receipt issuing, or external action |
| Automation and Integration adapter readiness | Prepare adapter evidence packets and command boundaries for Maven/Invoice4U, email, bank, OCR, and other external systems | Adapter task stays downstream of owning Domains and cannot mutate Commercial, Financial, or Inventory state without approval | Explicit approval before any external call, Apps Script/AppSheet/Sheets/Drive/email action, secret placement, or executor implementation |
| BusinessCase runtime generalization, if needed | Generalize case source handling only if a future ERP capability proves ServiceReport-derived cases are too narrow | BusinessCase remains an orchestration layer and does not become document/payment/inventory/approval owner | Selection required; schema and DB changes remain separately gated |
| Maven External Adapter readiness, if explicitly selected | Validate Maven customer/document/item matching, API contract evidence, secret placement, or source row-count/schema evidence as an adapter-specific task | Evidence packet exists with remaining blockers before real execution and does not redefine the project critical path | Explicit approval before any Maven/Invoice4U call, Apps Script change, secret placement, DB write, or executor implementation |
| Executive Orchestrator cycle | Run every selected task through Understand, Discover, Consult, Score, Decide, Execute, Validate, Learn, and Improve | Decision output includes consulted agent opinions, Executive Scores, duplicate-prevention proof, selected path, authority level, and Reality Check KPIs | No approval for read-only/documentation AUTO_EXECUTE work; approval required for any gated system or business decision |
| Orchestrator Decision Engine intake | Run every selected task, idea, bug, feature, investigation, proposal, or request through the existing `ORCHESTRATOR_AGENT` Project Executive matrix before work begins | Decision output identifies whether the task should exist, what can be reused, which specialist owns it, required/optional/forbidden tools, business evidence priority, architecture impact, authority level, validation, rollback, and whether the result adds a capability or only a document | No approval for read-only/documentation AUTO_EXECUTE work; approval required for any gated system or business decision |
| Maven/Invoice4U lifecycle tracking shell | Add read-only lifecycle tracking placeholders for Maven/Invoice4U state without sending or creating documents | Existing models/routes are used or shell is empty-state-first; no Maven/Invoice4U production action occurs | Selection/approval required before starting; explicit approval required before any Maven/Invoice4U write or integration action |
| AI Draft Recommendation Readiness approvals | Completed: Liad decisions recorded for SCR Small Service kit, Labor + Service, Technician Visit / Travel, Large Service oil, and partial serial confidence | AI Draft is `READY_FOR_APPROVAL_BASED_DRAFTS`; no document is created; future generated draft still requires approval flags and manual confirmation where required | Complete for documentation; explicit approval required before any implementation or write/action |
| Build hygiene for Playwright dependency gap | Resolve the existing full TypeScript/build blocker caused by missing `playwright` dependency/type declarations, if selected | Full TypeScript/build checks no longer fail on unrelated Playwright scripts | Selection/approval required before dependency or script-scope changes |
| Optional Wave 2 import approval package | Convert existing Wave 2 dry-run blockers into an approval-ready decision package only if explicitly selected | Package lists duplicate SKU decision, unresolved example PartUsed row, AI draft CustomerID mismatch, BusinessDocumentLog header shift, enum/status mappings, recommended choices, risks, and exact no-write next steps | Selection/approval required before starting; yes before any import, DB write, schema change, env change, or source-system action |
| Wave 2 staging import approval | Import approved Wave 2 data into Supabase staging only after blocker resolution and explicit approval | Dry-run blockers are resolved or explicitly accepted; source mappings are approved; no production, source-system, Prisma migration, schema push, or Wave 3 action occurs | Yes before any Wave 2 import |
| Second-stage Maven dry-run discovery | Confirm all Maven-origin Sheets and their links before import | Known `InvoiceMaven*` tabs plus any other Sheets tabs storing Maven imported/synced/created data are documented with purpose, target table, Customer/BusinessDocument/Product links, and active V1/later V1/future-historical classification | Yes before Maven history import |
| Import Waves execution planning | Sequence source imports by dependency wave | Agent-readable blocks define `WAVE_1_CORE`, `WAVE_2_SERVICE_WORKFLOW`, `WAVE_3_MAVEN_DATA`, and `WAVE_4_EXTENDED_OPERATIONS` with owners, dependencies, blockers, forbidden actions, and success criteria | Yes before real import |
| Supabase production shadow setup | Create `talcompressors-next-prod` only after staging validation passes | Production shadow project exists; no production cutover and no AppSheet/Sheets/Maven changes | Yes after staging validation |
| Real staging import | Import validated V1 data into Supabase staging | Requires approved dry-run report; legacy/test `ReportEquipmentItems` rows remain excluded by design | Yes before running import |
| Server Actions architecture | Make internal Next.js write flows use Server Actions by default | Approvals, AI draft approval, BusinessDocument creation, ServiceReport shadow updates, import review, queue commands, PostgreSQL mutations, and offline sync actions have Server Action paths | Yes before write implementation |
| Offline queue/PWA sync | Support field work without internet | Local pending actions sync automatically; conflicts are logged for review and not silently overwritten | Yes before sync implementation |
| VPS/Remote Development planning | Select an open full development environment for remote work | Candidate supports terminal, full repo, Git/GitHub, Codex CLI, VS Code Server/browser IDE, Next.js, Prisma/PostgreSQL, future agents, SSH, mobile/tablet access, secrets, backups, and rollback | Yes before provisioning |

## DONE

| Task | Evidence |
|---|---|
| Asset Intelligence runtime implemented | Commit `58275e4 Add asset intelligence runtime`; `/equipment/[id]` now shows a Tal Intelligence Core Asset Intelligence panel with business objective, identity confidence, sources searched, relationship evidence, recurring service signals, data-quality gaps, recommended next action, and an asset service timeline. Runtime reuses existing `ReportEquipmentItems`, linked `ServiceReports`, linked `Customers`, and linked `BusinessDocuments`; it is read-only and does not select SKUs, mutate inventory, create documents, write DB rows, call Maven/Invoice4U, email customers, change schema, install packages, or expand architecture. Validation passed with `npx.cmd tsc --noEmit --pretty false --incremental false`, `npm.cmd run build`, `git diff --check`, and read-only Prisma count/sample validation (`ReportEquipmentItems=75`, `ServiceReports=63`, `BusinessDocuments=10`, sample equipment row `974cd590` linked to ServiceReport `5826`). Completion remains 81%; this increases operational intelligence inside existing Wave 1/Service Intelligence surfaces without changing the transitional percentage model |
| Tal Intelligence Core architecture documented | Commit `92cbcbf Add Tal Intelligence Core architecture`; added `project-brain/TAL_INTELLIGENCE_CORE.md` as the binding central intelligence architecture for business intelligence, AI recommendations, drafts, service analysis, inventory logic, customer history, pricing logic, and knowledge learning. Added the Capability Registry rule, Knowledge Governance levels, Explainability rule, Continuous Learning Engine, and Business Strategy Intelligence capability. Referenced the document from `PROJECT_OPERATING_PROTOCOL.md` and `PROJECT_INDEX.md`. Validation: `git diff --check` passed with line-ending warnings only; diff review confirmed no app code, Prisma schema, DB write/import, Maven/Invoice4U, email/customer action, inventory mutation, Apps Script, AppSheet, Google Sheets, Drive, source-system, package, deletion/move, or production action occurred. Completion remains 81% because this is architecture documentation, not a new runtime capability |
| Tal Executive Runtime Observe validation completed | Commits `e22dfcc`, `ceb4f26`, `4a72b55`, and `783765d` created and cleaned up a read-only POC under `tools/tal-executive-runtime/`. The Observe command collects Git, Project Brain, PROJECT_SYNC, runtime-map, and company-objective evidence before any AI decision. Latest validation with `C:\Users\משתמש\AppData\Local\Programs\Python\Python312\python.exe tools/tal-executive-runtime/executive_runtime.py --observe --dry-run` returned `Overall Observe Status: GREEN`, company objectives `USED`, missing evidence `none`, and stale/conflict warnings `none`. No OpenAI API call, Codex execution, DB write, Git mutation, Maven/Invoice4U, email/customer action, inventory mutation, production action, package install, or `app/**`, `lib/**`, `prisma/**` change occurred |
| TypeScript pricing-evidence typing issue fixed | Updated `app/ai-drafts/ai-draft-adapter.ts` so pricing evidence arrays are built with non-null `flatMap` construction instead of nullable `.map(...).filter(...)` chains. Business logic is unchanged: null-price rows are still skipped and the same pricing priority, source, confidence, and note values are emitted. Validation passed with `npx.cmd tsc --noEmit --pretty false --incremental false` and `git diff --check`. No Maven/Invoice4U, email/customer action, inventory mutation, schema change, package install, DB write, or external action occurred |
| ServiceReport to BusinessDocument internal draft refresh implemented | Added an explicit internal refresh action on the BusinessDocument review page. Validation used ServiceReport `5864` / `acd1133d` and existing BusinessDocument `NEXT-BD-DRAFT-acd1133d-QUOTE`: before refresh the draft had 2 lines; after refresh the same BusinessDocument ID remained, status stayed `WAITING_USER_APPROVAL`, 6 Hebrew maintenance lines were present (`מסנן אוויר`, `מסנן שמן`, `מפריד שמן`, `שמן / נוזל קירור למדחס SCR`, `ביקור טכנאי ונסיעה`, `עבודת טכנאי ושירות`), `מפריד שמן` stayed price-review required, Hebrew title/summary/lines were visible through the review view model, no AutomationCommand existed for the document, and no Maven/Invoice4U, email/customer action, inventory mutation, schema change, AppSheet/Apps Script/Google Sheets/Drive action, package install, or production action occurred. The former known unrelated `app/ai-drafts/ai-draft-adapter.ts` pricing-evidence typing issue is now resolved by the TypeScript pricing-evidence fix |
| Project Sources Sync Layer created | Committed `DOC_SYNC`; added `PROJECT_SYNC_STATE.md`, `PROJECT_SYNC_DELTA.md`, `PROJECT_SYNC_AGENTS.md`, `PROJECT_SYNC_TASKS.md`, `PROJECT_SYNC_AUTHORITY.md`, and `PROJECT_SYNC_OPERATING_GUIDE.md` as concise ChatGPT Project Sources files. They summarize current project state, latest delta, agent/tool/skill inventory, current/next tasks, authority matrix, and operating guide without replacing Project Brain. Validation confirmed sync files exist, required delta/agent/task/authority/guide coverage is present, every sync-file section heading has a parenthetical explanation line, and `git diff --check` passes with line-ending warnings only. No runtime feature work, schema change, DB write/import, package install, external action, Maven/Invoice4U, email/customer action, inventory mutation, source-system action, or production action occurred. Completion remains 81% because this is synchronization/documentation, not a new ERP runtime capability |
| Production Draft Generation maintenance quality fix implemented | Committed in `77cf9cd Finalize service report draft runtime quality updates`; no uncommitted runtime maintenance files remain. `lib/business-document-production-draft.ts` now detects `Small Service` from 2000/2500 hour evidence and `Large Service` from 4000/5000 hour evidence, adds standard maintenance parts as separate reviewable lines even when exact registry/price evidence is missing, checks ProductCatalog and PartsUsed read-only evidence, records detected maintenance type in recommendation/raw evidence, and exposes maintenance type in the internal BusinessCase recommendation panel. Validation on ServiceReport `5807` / `2bfc0748` generated Air Filter, Oil Filter, 3L SKR oil top-up, Technician Visit / Travel, and Labor + Service. Validation on ServiceReport `5864` / `acd1133d` generated Air Filter, Oil Filter, Oil Separator, oil/coolant, Technician Visit / Travel, and Labor + Service; Oil Separator stayed `Needs Price Review`. Focused TypeScript passed for touched files; at that closeout, full TypeScript still failed only on the known unrelated `app/ai-drafts/ai-draft-adapter.ts` pricing-evidence typing issue, which is now resolved by the TypeScript pricing-evidence fix. Side-effect counts remained unchanged: BusinessDocuments `10`, BusinessDocumentItems `21`, AutomationCommands `1`, InventoryTransactions `0`, EmailLogs `0`. No schema change, DB import/write, Maven/Invoice4U, email/customer action, inventory mutation, source-system action, external action, or production action occurred. Completion remains 81% because this is quality hardening of an existing Wave 3 capability |
| Autonomous Business Draft Generation Sprint 12 implemented | Commit `5313aec Improve autonomous business draft generation`; production draft generation now produces professional Hebrew business-facing titles, summaries, line names, explanations, confidence labels, knowledge-used evidence, and missing-evidence messages. It keeps English only for allowed technical identifiers and preserves existing Draft Gateway/idempotency boundaries. Hebrew display names are separated from canonical aliases so older approved English draft lines remain reusable as pricing evidence. Validation created real internal QUOTE drafts `NEXT-BD-DRAFT-3a4a3a99-QUOTE`, `NEXT-BD-DRAFT-5bc071f5-QUOTE`, and `NEXT-BD-DRAFT-2bfc0748-QUOTE`; repeated gateway calls returned `created=false` for all three. Stored checks confirmed Hebrew titles, Hebrew line items, review-required policy where needed, zero AutomationCommands, zero InventoryTransactions, zero EmailLogs, and no exposure of manufacturer SKU `901165`. At Sprint 12 closeout, focused TypeScript/build still failed only on the known unrelated `app/ai-drafts/ai-draft-adapter.ts` pricing-evidence typing issue; that blocker is now resolved by the TypeScript pricing-evidence fix. Local Next route validation was blocked by child-process DB connectivity to the Supabase pooler, while direct Prisma and gateway validation succeeded. No schema change, enum extension, migration, package install, Maven/Invoice4U execution, email/customer action, inventory mutation, OCR, bank API, receipt issuing, external adapter call, cloud/source-system action, or production behavior occurred. Completion moves to 81% |
| Production Draft Review and Learning Loop Sprint 11 implemented | Commit `782d44d Add production draft learning loop`; policy update commit `467f693 Add internal action policy automation`; added `lib/business-document-learning-boundary.ts`, extended BusinessDocument detail with `learningReview`, rendered Production Draft Review and Learning on `/business-documents/[id]`, enriched the existing internal approval audit log with structured learning evidence only for production-generated drafts, and added internal action policy states `AUTO_ALLOWED`, `REVIEW_REQUIRED`, `APPROVAL_REQUIRED`, and `BLOCKED`. Policy-safe ServiceReport -> BusinessDocument draft generation no longer asks for the old approval phrase or review checklist; generated internal drafts are created automatically/idempotently when safe and remain review-required when confidence/evidence/line policy requires review. Validation created policy-safe draft `NEXT-BD-DRAFT-babbfe8a-QUOTE` without approval phrase, repeated submit returned `draftStatus=existing`, and side-effect counts were zero AutomationCommands, zero InventoryTransactions, and zero EmailLogs. Required Sprint 10 drafts validated: `NEXT-BD-DRAFT-acd1133d-QUOTE`, `NEXT-BD-DRAFT-891a1dd7-QUOTE`, and `NEXT-BD-DRAFT-e645e483-QUOTE` all render title quality, line quality, price confidence, missing evidence, and recommended corrections. Existing line-resolution and approval forms corrected and approved `NEXT-BD-DRAFT-e645e483-QUOTE`; final lines are `Technician Visit / Travel` `1 x 300 = 300` and `Labor + Service` `2 x 275 = 550`, both `needsPriceApproval=false`, with approved learning evidence recorded and `reusableByFutureDraftGeneration=true`. Existing 5806 totals stayed `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; manufacturer SKU `901165` was not exposed. TypeScript still fails only on the known unrelated AI Draft pricing-evidence issue. No schema change, enum extension, migration, package install, Maven/Invoice4U execution, email/customer action, inventory mutation, OCR, bank API, receipt issuing, external adapter call, cloud/source-system action, or production behavior occurred. Completion remains 80% |
| Production Draft Generation Sprint 10 implemented | Commit `07a3a7b Add production draft generation`; added `lib/business-document-production-draft.ts` and wired `/business-cases/service-report/[id]` so BusinessCase draft creation now exhausts existing customer, compressor, manufacturer, service interval, service-kit, approved BusinessDocumentItem, and approved correction evidence before marking fields unknown. The page shows Production Draft Recommendation, confidence, Knowledge First evidence, generated lines, and missing evidence before approval. The action still routes Business Intent -> Domain Policy -> existing BusinessDocument Draft Gateway; the gateway remains the only writer and ServiceReport + DocumentType idempotency remains active. Validation created real non-5806 QUOTE drafts `NEXT-BD-DRAFT-acd1133d-QUOTE`, `NEXT-BD-DRAFT-891a1dd7-QUOTE`, and `NEXT-BD-DRAFT-e645e483-QUOTE`; repeated submit for `e645e483` returned `draftStatus=existing` and count stayed `1`. The corrected source mapping stores general approved historical evidence as `SIMILAR_SERVICE_HISTORY`, not same-customer history. Route validation passed for `/business-cases/service-report/e645e483`, `/operations`, `/customers/18779`, `/business-documents/NEXT-BD-DRAFT-e645e483-QUOTE`, `/preview`, `/pdf`, and 5806 regression review/preview/PDF. Existing 5806 totals stayed `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; manufacturer SKU `901165` was not exposed. Side-effect check for `NEXT-BD-DRAFT-e645e483-QUOTE` found zero AutomationCommands, zero InventoryTransactions, and zero EmailLog rows. TypeScript still fails only on the known unrelated AI Draft pricing-evidence issue. No schema change, enum extension, migration, package install, Maven/Invoice4U execution, email/customer action, inventory mutation, OCR, bank API, receipt issuing, external adapter call, cloud/source-system action, or production behavior occurred. Completion moves to 80% |
| Business Intent Policy Layer Sprint 9 implemented | Commit `5e8a504 Add business intent policy layer`; added `lib/business-intent-policy.ts` above the existing BusinessDocument Draft Gateway. `/business-cases/service-report/[id]` now requires Business Intent before draft creation and applies Domain Policy before the gateway can create an internal draft. Initial intents are `PROPOSE_WORK`, `BILL_CUSTOMER`, `COLLECT_PAYMENT`, `CREDIT_CUSTOMER`, `PURCHASE_PARTS`, `DELIVER_GOODS`, `WARRANTY_REVIEW`, and `SCHEDULE_FOLLOW_UP`. Allowed now: `PROPOSE_WORK -> QUOTE` and `BILL_CUSTOMER -> INVOICE`; blocked now: payment without FinancialEvidence, credit without source BusinessDocument, purchase/delivery without future owning-domain runtime/schema support, and warranty/follow-up with no document draft by default. Validation reused `NEXT-BD-DRAFT-9981b978-QUOTE`, created/reused `NEXT-BD-DRAFT-9981b978-INVOICE`, and confirmed blocked intents redirect with `draftGatewayStatus=intent-blocked`. Routes passed for BusinessCase, Operations, Customer 360, both non-5806 draft review/preview/PDF routes, and the 5806 regression routes. Existing 5806 totals stayed `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; manufacturer SKU `901165` was not exposed. Read-only Prisma check found exactly the two target drafts for ServiceReport `5802`, each with one item, zero AutomationCommands, and zero InventoryTransactions. TypeScript still fails only on the known unrelated AI Draft pricing-evidence issue. No schema change, enum extension, migration, package install, Maven/Invoice4U execution, email/customer action, inventory mutation, OCR, bank API, receipt issuing, external adapter call, cloud/source-system action, or production behavior occurred. Completion moves to 79% |
| Generalized BusinessDocument Draft Gateway Sprint 8 implemented | Commit `36299a8 Add business document draft gateway`; added `lib/business-document-draft-gateway.ts`, refactored existing AI Draft Preview creation to delegate to the gateway, and added a protected BusinessCase-level gateway form on `/business-cases/service-report/[id]`. The gateway requires selected current-schema document type, exact phrase `CREATE BUSINESS DOCUMENT DRAFT`, complete Business Intelligence, pricing review, confidence review, missing-evidence review, and pricing override when lines are review-required. Idempotency is ServiceReport + DocumentType through deterministic id `NEXT-BD-DRAFT-{ServiceReportAppSheetId}-{DocumentType}` plus defensive `serviceReportId + documentTypeSelected` lookup. Approved write validation created non-5806 ServiceReport `5802` / `9981b978` / `8d889aeb-61ab-4b66-9316-516af5cd683c` as `NEXT-BD-DRAFT-9981b978-QUOTE`; first submit changed count `0 -> 1`, second submit returned the same draft and count stayed `1`. Created draft has one review-required line, gateway audit log, `WAITING_USER_APPROVAL`, `PENDING`, zero AutomationCommands, and zero InventoryTransactions. Routes passed for `/business-cases/service-report/9981b978`, `/operations`, `/customers/18779`, `/business-documents/NEXT-BD-DRAFT-9981b978-QUOTE`, `/preview`, and `/pdf`. Existing 5806 routes and totals stayed valid; manufacturer SKU `901165` was not exposed in checked 5806 review/preview HTML. TypeScript still fails only on the known unrelated AI Draft pricing-evidence issue. No schema change, migration, package install, Maven/Invoice4U execution, email/customer action, inventory mutation, OCR, bank API, receipt issuing, external adapter call, cloud/source-system action, or production behavior occurred. Completion moves to 78% |
| End-to-End Service Flow MVP Sprint 7 implemented | Commit `e8cb765 Add end-to-end service flow projection`; added a read-only service-flow projection to BusinessCase and rendered it on `/business-cases/service-report/[id]`. The page now shows Completed Service Report -> BusinessCase -> Business Intelligence Analysis -> Business Suggestions -> Commercial Runtime -> BusinessDocument Draft -> Operations Command Center -> Customer 360 -> Approval Ready. Business Intelligence answers what happened, evidence exists, evidence is missing, recommended business action, recommended BusinessDocument type, approvals required, financial implications, and inventory implications. Suggestions remain internal and no execution occurs. Validation: project TypeScript still fails only on pre-existing unrelated AI Draft pricing-evidence typing issues; `git diff --check` passed with CRLF warnings only; unsandboxed read-only route validation passed for `/service-reports/1e25bbb1`, `/business-cases/service-report/1e25bbb1`, `/operations`, `/customers/18953`, `/business-documents/NEXT-AI-DRAFT-5806`, `/business-documents/NEXT-AI-DRAFT-5806/preview`, and `/business-documents/NEXT-AI-DRAFT-5806/pdf`; content checks confirmed the full service flow, Business Intelligence Analysis, Business Suggestions, approval readiness, Operations Command Center context, Customer 360 context, linked BusinessDocument draft explanation, selected-type explanation, and `NEXT-AI-DRAFT-5806`; totals stayed `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; manufacturer SKU `901165` was not exposed in checked review/preview HTML. No schema change, Prisma migration, DB write, package install, OCR, external AI, Maven/Invoice4U execution, email/customer action, inventory mutation, cloud/source-system action, production behavior, or new business engine occurred. Completion moves to 77% |
| Customer 360 Workspace Sprint 6 implemented | Commit `32bb35e Add Customer 360 workspace`; transformed `/customers/[id]` from a read-only customer record into Customer 360, a read-only projection that answers what is happening with the customer right now. It reuses existing Customer, BusinessCase Runtime, ServiceReport, BusinessDocument, Commercial lifecycle, Financial Intake, Approval, Automation, Asset, Timeline, and read-only Inventory-impact state. The workspace shows Customer Summary, Assets, Open Business Cases, Open Commercial Documents, Financial Status, Current Blockers, Recommended Next Action, Recent Timeline, and Future Opportunities. Financial/document amounts use BusinessDocument ViewModel totals. Validation: project TypeScript still fails only on pre-existing unrelated AI Draft pricing-evidence typing issues; `git diff --check` passed with CRLF warnings only; unsandboxed read-only route validation passed for `/customers/18953`, `/customers`, `/operations`, `/business-cases/service-report/1e25bbb1`, `/business-documents/NEXT-AI-DRAFT-5806`, `/business-documents/NEXT-AI-DRAFT-5806/preview`, and `/business-documents/NEXT-AI-DRAFT-5806/pdf`; PDF remained `application/pdf` and `59730` bytes; content checks confirmed all required Customer 360 sections, BusinessCase link, BusinessDocument link, Financial status, Commercial status, no-CRM/read-only boundary text, and `2205.45 ILS`; totals stayed `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; manufacturer SKU `901165` was not exposed in checked review/preview HTML. No schema change, Prisma migration, DB write, package install, CRM workflow, OCR, bank API, receipt issuing, external accounting, Maven/Invoice4U call, email/customer action, inventory mutation, cloud/source-system action, production behavior, or writable customer workflow occurred. Completion moves to 76% |
| Operations Command Center Sprint 5 implemented | Commit `be1fada Add operations command center`; transformed `/operations` from a single-bucket dashboard into the read-only TAL Operations Command Center. It reuses existing BusinessCase Runtime, Commercial lifecycle, Financial Intake, Approval, Automation, and read-only Inventory-impact state. Queue rows are projections of existing BusinessCases, not persisted tasks. The screen now projects BusinessCases into My Work Today, Immediate Attention, High Priority, Waiting For Technician, Waiting For Customer, Waiting For Approval, Waiting For Financial Action, Waiting For Parts, and Ready To Close; rows show customer, asset, BusinessCase, current stage, blocker, next recommended action, priority, assigned owner, last activity, commercial status, financial status, and internal Open Action link. Validation: project TypeScript still fails only on pre-existing unrelated AI Draft pricing-evidence typing issues; `git diff --check` passed with CRLF warnings only; unsandboxed read-only route validation passed for `/operations`, `/business-cases/service-report/1e25bbb1`, `/business-documents/NEXT-AI-DRAFT-5806`, `/business-documents/NEXT-AI-DRAFT-5806/preview`, and `/business-documents/NEXT-AI-DRAFT-5806/pdf`; PDF remained `application/pdf` and `59730` bytes; content checks confirmed all required command-center queues, row fields, Open Action, BusinessCase link, Financial status, Commercial status, and queue/no-execute boundary text; totals stayed `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; manufacturer SKU `901165` was not exposed in checked review/preview HTML. No schema change, Prisma migration, DB write, package install, OCR, bank API, receipt issuing, external accounting, Maven/Invoice4U call, email/customer action, inventory mutation, cloud/source-system action, production behavior, or writable task manager occurred. Completion moves to 75% |
| Operations Center Sprint 4 implemented | Commit `c860365 Add operations center workspace`; added `/operations` as the read-only daily TAL Operations Center. It reuses BusinessCase Runtime through new `getBusinessCaseList()`, Commercial lifecycle, Financial Intake, Approval, and Automation runtime. The view groups BusinessCases into Requires Immediate Action, Waiting For Technician, Waiting For Customer, Waiting For Internal Approval, Waiting For Financial Action, Waiting For External Adapter, Ready To Close, and Closed Recently; rows show customer, asset, BusinessCase, current stage, blocker, next recommended action, assigned owner, priority, last activity, financial status, and commercial status. Validation: project TypeScript still fails only on pre-existing unrelated AI Draft pricing-evidence typing issues; `git diff --check` passed with CRLF warnings only; unsandboxed read-only route validation passed for `/operations`, `/business-cases/service-report/1e25bbb1`, `/business-documents/NEXT-AI-DRAFT-5806`, `/business-documents/NEXT-AI-DRAFT-5806/preview`, and `/business-documents/NEXT-AI-DRAFT-5806/pdf`; PDF remained `application/pdf`; content checks confirmed all required buckets, next recommended action, BusinessCase link, Financial status, Commercial status, and no-execute boundary text; totals stayed `2205.45 ILS`; manufacturer SKU `901165` was not exposed in checked review/preview HTML. No schema change, Prisma migration, DB write, package install, OCR, bank API, receipt issuing, external accounting, Maven/Invoice4U call, email/customer action, inventory mutation, cloud/source-system action, or production behavior occurred. Completion moves to 74% |
| Financial Capability Sprint 3 implemented | Commit `069f34c Add financial intake capability`; extended `lib/financial-intake-boundary.ts` into an internal Financial Intake capability boundary with FinancialEvidence source types, evidence drafts, extraction fields, confidence, attachment ID, matching, approval review, receipt draft, and tax invoice / receipt draft. BusinessDocument detail now exposes `financialIntake`; `/business-documents/[id]` shows a read-only Financial intake and settlement panel; BusinessCase financial status now summarizes Financial Intake evidence/matching. Validation: project TypeScript still fails only on pre-existing unrelated AI Draft pricing-evidence typing issues; `git diff --check` passed with CRLF warnings only; unsandboxed read-only route validation passed for `/service-reports/1e25bbb1`, `/business-cases/service-report/1e25bbb1`, `/business-documents/NEXT-AI-DRAFT-5806`, `/business-documents/NEXT-AI-DRAFT-5806/preview`, `/business-documents/NEXT-AI-DRAFT-5806/pdf`, and `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806`; PDF remained `application/pdf` and `59807` bytes; review content showed Payment evidence, Matching, Financial approval boundary, Receipt draft, Tax Invoice / Receipt draft, manual-entry evidence draft, draft-ready state, and issuing-blocked state; BusinessCase financial status updated; totals stayed `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; manufacturer SKU `901165` was not exposed in checked review/preview HTML. No schema change, Prisma migration, DB write, package install, OCR, bank API, receipt issuing, external accounting, Maven/Invoice4U call, email/customer action, inventory mutation, cloud/source-system action, or production behavior occurred. Completion moves to 73% |
| Commercial Lifecycle Hardening Sprint 2 implemented | Commit `c8d8c6c Harden commercial document lifecycle`; added `lib/business-document-commercial-lifecycle.ts` as a derived Commercial lifecycle boundary and extended BusinessDocument detail with `commercialLifecycle`. The review page now shows Draft, Internal Review, Needs Changes, Approved, Ready For External Adapter, Externally Processed, Financial Pending, Financial Completed, and Closed stages with current stage, next transition, blockers, and boundary text. BusinessCase Commercial summary now uses the derived lifecycle stage. Existing BusinessDocument Runtime, ViewModel, preview, PDF, Approval Boundary, Automation Boundary, Financial Boundary, Review Boundary, approval controls, AutomationCommand gate, and line resolution controls remain in place. Validation: project TypeScript still fails only on pre-existing unrelated AI Draft pricing-evidence typing issues; `git diff --check` passed with CRLF warnings only; unsandboxed read-only route validation passed for `/service-reports/1e25bbb1`, `/business-cases/service-report/1e25bbb1`, `/business-documents/NEXT-AI-DRAFT-5806`, `/business-documents/NEXT-AI-DRAFT-5806/preview`, `/business-documents/NEXT-AI-DRAFT-5806/pdf`, and `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806`; PDF remained `application/pdf` and `59807` bytes; lifecycle content showed `Ready For External Adapter`; approval/return/automation/line-resolution controls remained visible; totals stayed `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; manufacturer SKU `901165` was not exposed in checked review/preview HTML. No schema change, Prisma migration, DB write, package install, Maven/Invoice4U call, email/customer action, inventory mutation, cloud/source-system action, or production behavior occurred. Completion moves to 72% |
| BusinessCase Runtime Sprint 1 implemented | Commit `f420701 Add BusinessCase runtime`; added `app/business-cases/business-case-runtime.ts` and `/business-cases/service-report/[id]` as a read-only application/runtime BusinessCase concept derived from existing ServiceReport context. It reuses ServiceReport, BusinessDocument, Approval, AutomationCommand, Financial Intake placeholder, and Inventory Impact placeholder runtime, adds a ServiceReport detail link, and updates `APPLICATION_ROUTE_MAP.md`. BusinessCase orchestrates existing domains and does not own documents, payments, inventory, approvals, automation, or external adapters. Validation: project TypeScript still fails only on pre-existing unrelated AI Draft pricing-evidence typing issues; unsandboxed read-only route validation passed for `/service-reports/1e25bbb1`, `/business-documents/NEXT-AI-DRAFT-5806`, `/business-documents/NEXT-AI-DRAFT-5806/preview`, `/business-documents/NEXT-AI-DRAFT-5806/pdf`, `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806`, and `/business-cases/service-report/1e25bbb1`; PDF remained `application/pdf` and `59807` bytes; BusinessCase content showed Party/Customer, Assets, Service Operations, Commercial, Approval, Automation, Financial Intake, Inventory Impact, and Closure readiness; totals stayed `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; manufacturer SKU `901165` was not exposed in checked review/preview HTML. No schema change, Prisma migration, DB write, package install, Maven/Invoice4U call, email/customer action, inventory mutation, cloud/source-system action, or production behavior occurred. Completion moves to 71% |
| Domain Ownership Gate integrated | Added one permanent rule to existing `PROJECT_OPERATING_PROTOCOL.md` section `18B. TDOS Risk-Based Operating Model`. Future features, runtime changes, workflow changes, and architecture changes must identify Domain owner, owner/responsible agent, allowed interacting Domains, Domains that must not own/mutate the object or workflow, supporting source of truth, and boundary-preservation validation before implementation. If unclear, implementation stops for clarification. No new governance file, registry, runtime behavior change, schema change, DB write, package install, Maven/Invoice4U call, email/customer action, inventory action, cloud action, or production action occurred |
| TDOS frozen in Maintenance Mode | TDOS work is frozen for now and project focus returns to ERP capability implementation. Reopen TDOS only if ERP work is blocked by a missing TDOS capability, real Project Brain/runtime drift is detected, or Knowledge Release becomes required for active ChatGPT Project Sources workflow. Project Sources Publishing Pipeline / Knowledge Release is recorded as a future conditional TDOS improvement only; no publisher/runtime/script/manifest/new file/synchronization framework is approved now |
| Wave 3 Domain Boundary Refactoring completed | Commercial runtime boundary tightened without schema, DB, package, route, source-system, external, customer-facing, inventory, Maven/Invoice4U, cloud, or production changes. Extracted Financial payment/evidence parsing to `lib/financial-intake-boundary.ts`, Approval/Governance blocker/phrase logic to `lib/business-document-approval-boundary.ts`, Automation/Integration Maven command-gate logic to `lib/business-document-automation-boundary.ts`, and cross-domain review status/lifecycle/warnings to `lib/business-document-review-boundary.ts`. `app/business-documents/business-document-adapter.ts` and `app/business-documents/[id]/actions.ts` now delegate those responsibilities while preserving existing behavior. Focused TypeScript passed for touched files; repo-wide TypeScript still fails only on pre-existing unrelated `app/ai-drafts/ai-draft-adapter.ts` pricing-evidence typing issues. Unsandboxed read-only route validation passed for `/business-documents/NEXT-AI-DRAFT-5806`, `/business-documents/NEXT-AI-DRAFT-5806/preview`, and `/business-documents/NEXT-AI-DRAFT-5806/pdf`; totals stayed `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; manufacturer SKU `901165` was not exposed in checked review/preview HTML. Completion remains 70% |
| Financial Intake Engine design added to Document Engine KB | Extended `project-brain/DOCUMENT_ENGINE.md` only. Future payment/receipt intake architecture is now the Financial Intake Engine: `FinancialEvidence` -> Extraction Draft -> Matching Engine -> Payment Suggestion -> User Approval -> later Receipt or Tax Invoice / Receipt creation. `PaymentEvidence` remains only subtype/legacy wording when needed. Supported future evidence sources include check image, bank transfer proof, bank export, bank screenshot, PDF proof, email proof, manual entry, and future bank API. Extracted data is suggestion-only; duplicate detection, customer matching, BusinessDocument matching, open-balance matching, and partial-payment matching are required before approval. No runtime code, Prisma schema, DB write, package install, OCR implementation, bank API, external API, Maven/Invoice4U action, customer-facing action, inventory action, cloud change, source-system action, or production change occurred |
| Universal Business Document Engine foundation implemented | Added `lib/business-document-view-model.ts` as the internal document type configuration and `BusinessDocumentViewModel` boundary; `BusinessDocument` remains the source of truth; `/business-documents/[id]/preview` now consumes the view model for header/source/line/totals/SKU visibility while keeping the same PDF route over the preview. Validation: focused TypeScript passed for touched BusinessDocument files; outside-sandbox route checks returned 200 for `/business-documents/NEXT-AI-DRAFT-5806` and `/business-documents/NEXT-AI-DRAFT-5806/preview`; totals `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS` remained present; preview did not expose manufacturer SKU `901165`; `/business-documents/NEXT-AI-DRAFT-5806/pdf` returned 200 with `%PDF` magic and 59,807 bytes. BusinessDocument `NEXT-AI-DRAFT-5806` / ServiceReport `5806` remains a regression fixture only, not an architecture dependency. No schema change, DB write, package install, Maven/Invoice4U call, external API call, email/customer send, inventory action, source-system action, or production action occurred |
| Wave 3 Legacy Excel Parser Strategy completed | Commit `aab5de5 Document legacy Excel parser strategy`; planning-only pass compared accuracy, maintenance, automation, performance, and scalability for LibreOffice conversion, Node legacy `.xls` parsers, manual conversion/export, Python/openpyxl, Python legacy parser packages, and Excel COM automation. Preferred solution is approved LibreOffice headless conversion to `.xlsx` followed by existing OOXML extraction. Future import workflow now covers intake, classification, conversion, extraction, validation, versioning, review, runtime fixture use, DB-promotion gate, and update diffs. 20APM validation remains exact-source-only: source sheet/row and oil separator SKU required, compared against `20PM2` SKU `25350030-021`; missing evidence stays blocked. No package install, parser implementation, DB write, schema change, runtime behavior change, Maven/Invoice4U call, external API call, email/customer action, inventory action, source-system action, or production action occurred |
| EPM/APM Excel Parser Strategy completed | Commit `7981c2e Plan EPM xls parser strategy`; verified `soffice`/LibreOffice is not installed, Node is available, `python` is a Windows Store shim, and `py` is unavailable. Recommended approach is approved LibreOffice conversion from legacy binary `.xls` to `.xlsx`, followed by the existing read-only OOXML extractor. Documented validation gates for `20APM`: exact source sheet/row, oil separator SKU, comparison to `20PM2` SKU `25350030-021` from PM sheet `20PM2` row 8, conflict review, and no shared-kit inference from SKU overlap. No package install, DB write, schema change, runtime behavior change, Maven/Invoice4U call, email/customer action, inventory action, source-system action, or production action occurred |
| Manufacturer Registry Alias and Conflict Validation completed | Commit `b00195b Document manufacturer alias conflict rules`; verified generated PM fixture and Project Brain compatibility rules. Evidence found: `20PM2` oil separator SKU `25350030-021`, source sheet `20PM2`, row 8. Evidence missing/blocked: exact `20APM` registry row is not in the generated PM fixture; EPM/APM exact source-row extraction remains blocked by legacy binary workbook/tooling gap. Updated `SKU_MATCHING_RULES.md` and `MANUFACTURER_SERVICE_KITS.md` to make alias/model conflicts needs-review and to state shared SKU overlap does not imply shared service kit. No runtime behavior change, customer PDF change, DB write, schema change, package install, Maven/Invoice4U call, email/customer action, inventory action, source-system action, or production action occurred |
| Manufacturer Parts Registry Import + Service Kit Intelligence MVP completed | Commit `ce66cc9 Add manufacturer parts registry fixture`; generated `data-sources/vendor-spare-parts/generated/manufacturer-parts-registry.sample.json`, `service-kit-intelligence.sample.json`, `shared-sku-overlap.sample.json`, and `manufacturer-registry-import-summary.sample.json` from the PM Series workbook without DB writes. PM Series extraction found sheets/models `10PM2`, `15PM2`, `20PM2`, `30PM`, `40PM`, `50PM`, `60PM`, `75PM`, and `100PM`; captured 250 registry rows; derived 8 interval kit candidates per PM model; identified 69 shared manufacturer SKUs. EPM Series is legacy binary `.xls` and remains partial-only in this environment. Runtime now reads the generated registry fixture instead of hard-coded 40PM rows and keeps manufacturer SKU internal-only while customer preview/PDF uses only Tal/internal sales SKU. No schema change, DB write/import, package install, Maven/Invoice4U call, email/customer action, inventory action, source-system action, or production action occurred |
| Governance foundation completed | `PROJECT_OPERATING_PROTOCOL.md` exists and remains highest authority/governor |
| Project Brain startup alignment Phase 1 | Startup now begins with `PROJECT_INDEX.md`; retired current-task path is a stub |
| Next.js shadow app created | App exists as shadow/development implementation only |
| Hebrew RTL UI created | Next.js UI supports Hebrew RTL direction |
| ServiceReports list/detail created | Shadow app includes ServiceReports list and detail screens |
| Snapshot JSON adapter created | Adapter reads local snapshot JSON instead of production sources |
| Live read-only validation completed | Original baseline was `Customers_Final = 763`, `ServiceReports = 62`, `ReportEquipmentItems = 108`; Wave 1 baseline updated to `Customers_Final = 763`, `ServiceReports = 63`, `ReportEquipmentItems = 109` after read-only export validation found legitimate new business data |
| Narrow Phase 1 Prisma subset rejected | Documented as too narrow for Tal Operating System V1 |
| PostgreSQL V1 scope approved | `project-brain/migration/POSTGRESQL_V1_SCOPE.md` approved as final V1 scope before Prisma generation |
| Full Prisma schema created | `prisma/schema.prisma` created from the approved full V1 scope |
| Prisma schema committed and pushed | Commit `108b756 Add full PostgreSQL V1 Prisma schema` exists on `origin/main` |
| Prisma validation tooling added | Commit `00e2067 Add Prisma validation tooling` |
| Prisma schema validated | Prisma v6.19.3 validation passed with process-only placeholder `DATABASE_URL`; no DB connection or migration |
| Liad architecture decisions documented | Equipment-row exclusion, derived report counter, Server Actions-first, offline-first, and next implementation order recorded in Project Brain docs |
| Remote infrastructure requirement recorded | Future VPS/Remote Development track must use an open full dev environment and avoid restricted control panels/provider lock-in |
| Project Brain Consolidation Phase 1-3 completed | Startup path enforced through `PROJECT_INDEX.md`; retired current task references cleaned |
| Retired current task references cleaned | Commit `9433855 Clean retired current task references` |
| Startup and shutdown workflow enforcement implemented | Commit `e36c35e Implement hey codex and by codex workflow`; `hey codex` starts from `PROJECT_INDEX.md`; `by codex` runs closeout, approved commit, push, and next startup point |
| Reality Check Git sync hardening implemented | Commit `670f4c8 Harden codex startup and reality check sync`; `hey codex` fast-forwards from `origin/main` before Project Brain reads when clean; Reality Check compares live Git with Project Brain recorded commits |
| Two-commit Reality Check model implemented | Commit `315b8cc Fix reality check commit model`; governance tracks Last Implementation Commit separately from Last Closeout Commit to prevent closeout metadata loops |
| Project Brain commit model state synced | Commit `8114210 Sync project brain commit model state`; classified as closeout metadata/state sync only |
| Closeout commit loop prevention documented | Closeout-only metadata commits do not require another Project Brain sync just to record their own hash |
| Master map and agent routing added | Commit `2963977 Add master map and agent routing`; classified as governance implementation because it changed project routing behavior |
| Supabase staging-first shadow plan approved | Commit `d1d6f88 Document Supabase staging-first shadow plan`; use `talcompressors-next-staging` first and `talcompressors-next-prod` production shadow only after staging validation; local PostgreSQL is not first target |
| Staging env placeholder and secret ignore prepared | Commit `fc1dfa8 Prepare Supabase staging env placeholders`; `.env.staging.example` contains required env names only; `.gitignore` blocks real `.env` files while allowing examples; Supabase project creation remains pending authenticated access |
| Prisma schema reconciled for Supabase staging | Commit `9a81290 Reconcile Prisma schema for Supabase staging`; added `DIRECT_URL`, `ReportEquipmentItem.reportCounter`, and `@@index([reportCounter])`; validation passed with process-only placeholders |
| Supabase staging project and secrets prepared | `talcompressors-next-staging` exists; real `DATABASE_URL`, `DIRECT_URL`, and `NEXT_PUBLIC_APP_ENV` values are stored outside git in ignored local env storage |
| Prisma generate completed for staging | Prisma Client generated with local staging env values after explicit approval; no DB write was performed by generate |
| Supabase staging schema pushed | Staging-only `prisma db push` completed after explicit approval; no migration, seed, import, production action, AppSheet/Sheets/Maven action, or code change occurred |
| Staging schema verification completed | Read-only verification found 21 public V1 tables, no missing/extra V1 tables, and approved indexes/relations/enums present |
| ReportEquipmentItems exclusion terminology updated | Commit `b6b709b Reclassify ReportEquipmentItems exclusions`; 9 rows missing `ReportID` and 25 unmatched `ReportID` rows are classified as historical test data, not business data, no recovery required, excluded by design |
| Import Waves plan documented | Commit `9efa017 Refactor import planning and update Wave 1 baseline`; structured `WAVE_ID` blocks for Codex, AI agents, automation, and Project Brain indexing are recorded; Wave 1 baseline is `Customers_Final = 763`, `ServiceReports = 63`, `ReportEquipmentItems = 109` |
| Wave 1 staging import executed | 2026-06-22T12:54:25.974Z / 2026-06-22 15:54 IDT; source CSVs from `data-sources/exports/`; DB counts read back by Prisma Client: `customers = 763`, `service_reports = 63`, `report_equipment_items = 75`; excluded legacy/test rows: `9` missing `ReportID`, `25` unmatched `ReportID`; validation `PASS`; manifest/report generated under ignored local `data-sources/exports/import-runs/`; no source-system or production changes |
| Wave 1 import commit classified | Commit `3abf7d3 Record Wave 1 staging import pass`; classified as latest implementation/import commit |
| Wave 1 PostgreSQL read implemented | Commit `29331fb Read Wave 1 service reports from PostgreSQL`; Next.js service report list/detail screens read Wave 1 PostgreSQL staging data |
| Post-import Wave 1 review completed | `/service-reports` HTTP 200; `/service-reports/acd1133d` HTTP 200; 63 service report links rendered; counts `customers = 763`, `service_reports = 63`, `report_equipment_items = 75`; validation PASS; issues recorded for date, status, and sparse equipment display |
| Autonomous agent orchestration governance created | Commit `a28da7b Create autonomous agent orchestration governance`; Codex is the main Orchestrator for AUTO_ALLOWED work, routes tasks to existing agent owners, validates, updates Project Brain, commits/pushes safe scoped work, and stops only at APPROVAL_REQUIRED gates |
| Wave 1 read/display mapping fixes validated | Commit `fd76610 Fix Wave 1 service report display mapping`; service dates fall back to `raw_source` service date without DB writes; pending-signature source status maps to `Pending Signature`; missing source status displays as `Status Missing`; sparse equipment rows render safe fallbacks; read-only staging validation found 63 reports, 75 equipment rows, 0 unknown dates, 29 pending-signature statuses, 32 signed statuses, 2 status-missing rows, 0 unknown statuses, and 0 sparse fallback failures; read-only HTTP validation returned 200 for `/service-reports` and `/service-reports/acd1133d` with 63 links |
| Project tree reporting required | Commit `7f63193 Require project tree reporting`; every Reality Check, Approval Gate, Autonomous Completion Report, and `by codex` closeout must include Project Tree Position from `PROJECT_INDEX.md` |
| Wave 2 planning/discovery gate approved | Liad approved Wave 2 planning/discovery only; scope is documentation/source-readiness planning and excludes Wave 2 import, DB writes, schema changes, Maven actions, production shadow, and source-system changes |
| Wave 2 connector dry-run validation completed | 2026-06-22 19:13 IDT; connector-only read validation approved after blocked CSV export paths; all eight Wave 2 tabs were readable; observed rows: `ProductsCatalog = 113`, `PartsUsed = 1`, `AIDraftSuggestions = 2`, `BusinessDocuments = 1`, `BusinessDocumentItems = 0`, `BusinessDocumentLog = 2`, `ApprovalsLog = 0`, `EmailLog = 0`; read validation PASS, import readiness FAIL/BLOCKED; no Google Sheets writes, AppSheet changes, Maven changes, Apps Script changes, DB writes, Supabase writes, imports, Prisma commands, or production actions |
| Staging connectivity diagnostics completed | 2026-06-22; Next.js starts and home page returns HTTP 200, but Prisma cannot reach the Supabase pooler; `/service-reports` and `/service-reports/acd1133d` return HTTP 500; `.env.staging` matches verified Supabase Project ID `mdlvxklufrchiabonafm`; correction source: verified visually from Supabase Project Settings; after temporary `.env` creation, Prisma validation passed and `db pull --print` moved to `P1001` connectivity against `aws-1-eu-central-1.pooler.supabase.com:5432`; no DB writes/imports/migrations/source-system/production actions |
| Staging connectivity diagnosis resolved | Real read-only Prisma connectivity passed outside the network sandbox: `customer.count() = 763`; `npx.cmd prisma db pull --print` exit code `0`; earlier `P1001` was sandbox/runtime network limitation, not Supabase/project/env issue; no DB writes or migrations were run |
| Customers read-only module implemented | Commit `45da4d0 Implement customers read-only module`; added `/customers` list, `/customers/[id]` detail, customer adapter, active dashboard card, search/filter fields, service-report links, and future link counts for BusinessDocuments, AI Drafts, and Maven documents; validation: scoped TypeScript passed and `/`, `/customers`, `/customers/186DD`, `/service-reports`, `/service-reports/acd1133d` returned HTTP 200; no schema/env/migration/DB write/source-system/production changes |
| Automatic Project Brain closeout sync required | Commit `4ed6ca2 Require automatic project brain closeout sync`; `AGENTS.md`, `PROJECT_OPERATING_PROTOCOL.md`, and `PROJECT_INDEX.md` require Project Brain updates before every final report after a completed task; required fields are completed work, commit hash, validation, current blocker or `none`, exact next task, approval gates, and project completion percentage; validation `git diff --check` passed with CRLF warnings only; completion remains 50%; resolved blockers must be removed from current blocker state; no env/schema/migration/DB write/delete/move/remote/source-system/production changes |
| TDOS risk-based operating model integrated | Commit `7dc51f9 Integrate TDOS risk operating model`; `PROJECT_OPERATING_PROTOCOL.md` section `18B` now defines risk classes and required controls for `READ_ONLY_DISCOVERY`, `DOC_SYNC`, `SAFE_LOCAL_IMPLEMENTATION`, `SCHEMA_OR_DATA_CHANGE`, `EXTERNAL_SYSTEM_CHANGE`, `PRODUCTION_CHANGE`, and `ARCHITECTURE_CHANGE`; TDOS is not a standalone constitution or duplicate governance layer |
| Multi-agent operating workflow added | Commit `28f9bf2 Add multi-agent operating workflow`; created Builder, Map Guard, QA, Reviewer, Agent Communication Protocol, and Autonomous Build Workflow under `project-brain/agents/`; updated `AGENTS.md`, `PROJECT_OPERATING_PROTOCOL.md`, and `PROJECT_INDEX.md`; validation `git diff --check` passed with CRLF warnings only; completion remains 50%; no app/schema/migration/env/DB write/source-system/delete/move/remote/production changes |
| ReportEquipmentItems / Equipment read-only module implemented | Commit `3f1761f Add equipment read-only module`; added `/equipment`, `/equipment/[id]`, read-only `ReportEquipmentItem` adapter, dashboard activation, search/filter fields, and service-report links; validation: scoped TypeScript passed, `git diff --check` passed with CRLF warnings only, read-only Prisma lookup found equipment `3002f879`, and local HTTP checks returned 200 for `/`, `/equipment`, `/equipment/3002f879`, `/service-reports`, `/service-reports/5e0eaae3`, and `/customers`; no schema/env/migration/DB write/import/source-system/production changes; completion is 52% |
| PartsUsed read-only module implemented | Commit `14542b5 Add PartsUsed read-only module`; added `/parts-used`, `/parts-used/[id]`, read-only `PartUsed` adapter, dashboard activation, search/filter fields, service-report/product context links, and invalid enum filter hardening; validation: scoped TypeScript passed, `git diff --check` passed with CRLF warnings only, read-only Prisma validation found `parts_used = 0`, local HTTP checks returned 200 for `/`, `/parts-used`, `/parts-used?matchSource=BAD_VALUE`, `/service-reports`, `/equipment`, and `/customers`, and 404 for `/parts-used/not-a-real-part`; no schema/env/migration/DB write/import/source-system/production changes; completion is 53% |
| Application route map documented | Commit `7a8ce9b Add application route map`; created `APPLICATION_ROUTE_MAP.md` with every implemented route, module, status, data source, record count, and AppSheet equivalent; validation: actual `app/**/page.tsx` files enumerated, read-only Prisma counts verified, and `git diff --check` passed; no app/schema/env/migration/DB write/import/source-system/production changes |
| Data coverage audit completed | Commit `5a682ec Add data coverage audit`; created `DATA_COVERAGE_AUDIT.md` with read-only Prisma counts and classifications for 19 requested models; populated models are `Customer = 763`, `ServiceReport = 63`, and `ReportEquipmentItem = 75`; `PartUsed = 0` and `Product = 0` are empty; remaining workflow/runtime/Maven/sync/log tables are not ready for useful read-only module validation; no app/schema/env/migration/DB write/import/source-system/production changes |
| Service Report central work screen enhanced | Commit `71a5435 Enhance service report work screen`; `/service-reports/[id]` now includes customer summary, equipment summary, disabled future actions, lifecycle placeholders, and links to customer/equipment detail pages; validation: scoped TypeScript passed, `git diff --check` passed with CRLF warnings only, local route checks returned 200 for `/`, `/service-reports`, `/service-reports/acd1133d`, `/customers`, `/equipment`, and `/equipment/3002f879`, and detail HTML contained all requested labels/placeholders; no schema/env/migration/DB write/import/source-system/production changes; completion is 54% |
| Bidirectional module navigation implemented | Commit `8e3fae9 Add bidirectional module navigation`; Customer detail now explicitly labels linked Service Report work screens, Equipment detail explicitly links to the Service Report work screen, and Service Report detail continues linking back to Customer and Equipment detail pages; validation: scoped TypeScript passed, `git diff --check` passed with CRLF warnings only, outside-sandbox route checks returned 200 for `/`, `/customers`, `/customers/186DD`, `/equipment`, `/equipment/3002f879`, `/service-reports`, and `/service-reports/acd1133d`, and all four bidirectional link checks passed; no schema/env/migration/DB write/import/source-system/production/dashboard-route changes; completion is 55% |
| Service Reports list context enhancement implemented | Commit `9bd6435 Enhance service report list filters`; `/service-reports` now supports read-only search by report id/number, customer, technician, equipment/model/serial, status/customer/has-equipment filters, customer and equipment/model cues, preserved work-screen links, and an empty state; validation: scoped TypeScript passed, `git diff --check` passed with CRLF warnings only, outside-sandbox route checks returned 200 for `/`, `/service-reports`, `/service-reports?q=acd1133d`, `/service-reports?status=Signed`, `/service-reports?q=no-such-report-filter-value`, `/service-reports/acd1133d`, `/customers`, and `/equipment`, with control/cue/link/search/filter/empty-state checks passing; no schema/env/migration/DB write/import/source-system/production/route-map changes; completion is 56% |
| AI Draft Suggestions shell implemented | Commit `f8f271e Add AI draft suggestions shell`; added `/ai-drafts`, `/ai-drafts/[id]`, read-only `AiDraftSuggestion` adapter, active dashboard card, and route-map entries; table count is `0`, so the list is empty-state-first while detail mapping is ready for future rows; validation: read-only Prisma count confirmed `AiDraftSuggestion count = 0`, scoped TypeScript passed, `git diff --check` passed with CRLF warnings only, outside-sandbox route checks returned 200 for `/`, `/ai-drafts`, `/service-reports`, `/customers`, and `/equipment`, `/ai-drafts/not-a-real-draft` returned 404, and dashboard link/empty-state/document-type/approval checks passed; no schema/env/migration/DB write/import/source-system/production changes; completion is 57% |
| AI Draft sandbox runtime finding recorded | `/ai-drafts` HTTP 500 reproduced only inside the network sandbox due to Prisma being unable to reach the Supabase pooler; `/ai-drafts` returned HTTP 200 outside the sandbox, confirming no AI Draft code fix is required. Build remains blocked by unrelated missing `playwright` dependency/type declarations in `scripts/playwright/appsheet-discovery-auth.ts` |
| BusinessDocuments shell implemented | Commit `5fb5e20 Add business documents shell`; added `/business-documents`, `/business-documents/[id]`, read-only `BusinessDocument` adapter using items/log relations, active dashboard card, and route-map entries; table counts are `BusinessDocument = 0`, `BusinessDocumentItem = 0`, and `BusinessDocumentLog = 0`, so the list is empty-state-first while detail mapping is ready for future rows; validation: scoped TypeScript passed, `git diff --check` passed with CRLF warnings only, outside-sandbox route checks returned 200 for `/`, `/business-documents`, `/ai-drafts`, `/service-reports`, and `/customers`, `/business-documents/not-a-real-document` returned 404, and dashboard link/empty-state/Maven/AI-draft/approval checks passed; no schema/env/migration/DB write/import/Maven/Invoice4U/source-system/production changes; completion is 58% |
| Inventory stock route alias fixed | Commit `bd2bf19 Fix inventory stock route`; `/inventory-stock` now redirects to existing `/equipment`; validation returned 200 for `/`, `/inventory-stock` after redirect, `/equipment`, `/service-reports`, and `/customers`; no new data module, schema/env/migration/DB write/import/source-system/production changes; completion remains 58% |
| AutomationCommands shell implemented | Commit `1d5906c Add automation commands read-only shell`; added `/automation-commands`, `/automation-commands/[id]`, read-only `AutomationCommand` adapter, active dashboard card, and route-map entries; table count is `AutomationCommand = 0`, so the list is empty-state-first while detail mapping is ready for future command rows; validation: scoped TypeScript passed, `git diff --check` passed with CRLF warnings only, read-only Prisma count confirmed `AutomationCommand count = 0`, local route checks returned 200 for `/`, `/automation-commands`, `/service-reports`, and `/business-documents`, `/automation-commands/not-a-real-command` returned 404, and dashboard link/empty-state checks passed; no DB writes, command execution, Maven/Invoice4U/email action, schema change, migration, env change, import, source-system change, or production action occurred; completion is 59% |
| SCR matching preview panel implemented | Commit `0decfa2 Add SCR matching preview panel`; `/service-reports/[id]` now shows a read-only SCR matching preview panel for the documented report `1e25bbb1` / report `5806`, including detected model `SCR-40PM`, suggested SKUs `25200007-005` and `25100043-071`, quantities, confidence, price source, price approval flags, fixed visit `300 NIS`, and labor `275 NIS/hour` placeholder; unsupported reports show an unavailable state; validation returned 200 outside sandbox for `/`, `/service-reports`, `/service-reports/1e25bbb1`, `/customers`, and `/equipment`; no AI Draft creation, BusinessDocument creation, DB write/import, schema/env/migration change, Maven/Invoice4U action, source-system change, or production action occurred; completion was previously overstated as 60% and is corrected to 56% by the capability formula |
| Completion model corrected | `project-brain/FULL_PROJECT_DISCOVERY_AUDIT.md` found that the canonical 60% claim did not match the listed capability contributions. The corrected formula is Governance 15 + Supabase/Prisma 15 + Import/Wave 1 10 + Wave 1 UI 10 + Wave 2 6 + remaining pending capabilities 0 = 56. No evidence-based formula currently justifies 60%. No code, Prisma, DB, agents, planning systems, business logic, workflows, source-system, or production changes occurred |
| Parts / SKU Intelligence roadmap added | Reusable KBs `project-brain/SKU_MATCHING_RULES.md`, `project-brain/MANUFACTURER_PARTS_REGISTRY.md`, and `project-brain/SERVICE_COMMERCIAL_RULES.md` define read-only SKU candidate evidence, many-to-many model/SKU rules, generic equipment safeguards, SKU evidence order, confidence rules, and the hard inventory deduction gate; `project-brain/roadmap/ROADMAP.md` cross-references the layer. No implementation, Prisma, DB, import, source-system, Maven/Invoice4U, inventory transaction, or runtime workflow change occurred |
| Service pattern business rule documented | Liad-approved Large Service and Small Service rules are consolidated in `project-brain/MANUFACTURER_SERVICE_KITS.md`, `project-brain/SKU_MATCHING_RULES.md`, `project-brain/SERVICE_COMMERCIAL_RULES.md`, and `project-brain/DECISION_LOG.md`; rules support AI Draft expected line recommendations only and do not approve pricing, inventory deduction, BusinessDocument creation, Maven/Invoice4U action, DB writes, imports, Prisma changes, or runtime workflow changes |
| Global business document line rule documented | `project-brain/DOCUMENT_ENGINE.md`, `project-brain/SERVICE_COMMERCIAL_RULES.md`, `project-brain/MANUFACTURER_KNOWLEDGE_BASE.md`, `project-brain/SERVICE_PATTERN_INTELLIGENCE_REGISTRY.md`, `project-brain/AI_DRAFT_RECOMMENDATION_READINESS_DECISION_PACKET.md`, and `agents/MAVEN_AGENT.md` record that compressor service documents use Parts lines, Oil handling if needed, one `Labor + Service` line, and one `Technician Visit / Travel` line. No code, DB, Prisma, Maven, inventory, or runtime changes occurred |
| Email Document Intake Agent specified | `project-brain/EMAIL_DOCUMENT_INTAKE_AGENT_SPEC.md` defines planned/non-executable `EMAIL_DOCUMENT_INTAKE_AGENT` under `ORCHESTRATOR_AGENT`, with evidence packets for PO/RFQ/customer replies/document-chain matching; `agents/AGENT_REGISTRY.md`, `project-brain/roadmap/ROADMAP.md`, and `project-brain/DECISION_LOG.md` were updated. No Gmail runtime, email send, DB write, BusinessDocument creation, Maven action, inventory action, or automation runtime occurred |
| AI Draft readiness rules and email intake schema documented | Commit `be89380 Document AI draft readiness rules`; AI Draft is now `READY_FOR_APPROVAL_BASED_DRAFTS` for approval-based previews; added `project-brain/AI_DRAFT_READINESS_RECHECK.md` and `project-brain/EMAIL_INTAKE_EVIDENCE_PACKET_SCHEMA.md`; validation `git diff --check` passed with CRLF warnings only; no code, DB, Prisma, imports, Maven/Invoice4U, inventory, email runtime, source-system, customer-facing, or production workflow changes occurred |
| Orchestrator Decision Engine governance upgraded | `agents/ORCHESTRATOR_AGENT.md` now acts as the existing Project Executive for all task intake; `PROJECT_OPERATING_PROTOCOL.md` and `PROJECT_INDEX.md` require every future task to pass through the decision engine; `project-brain/DECISION_LOG.md` records the approved authority levels and reuse-before-create rule; no new PM Agent, duplicate governance, runtime implementation, DB, Prisma, Maven, Inventory, or customer-facing action occurred |
| Executive Orchestrator Phase 2 governance upgraded | Commit `76a9ba6 Upgrade orchestrator executive decision engine`; `agents/ORCHESTRATOR_AGENT.md` now requires the full Executive Cycle, consultation summaries from relevant existing agents, Executive Scoring, duplicate-proof gates before any new Agent/Registry/Spec/Rule/Knowledge Base/Roadmap Item, self-improvement review after every completed task, and Reality Check KPI reporting. `PROJECT_OPERATING_PROTOCOL.md`, `PROJECT_INDEX.md`, `project-brain/DECISION_LOG.md`, `project-brain/CURRENT_TASK.md`, and `project-brain/TASK_BOARD.md` were extended; no new Executive Agent, runtime implementation, DB, Prisma, Maven, Inventory, or customer-facing action occurred |
| Project Execution Mode transitioned to capability building | Commit `c205dfb Transition project to capability building`; project mode is now `CAPABILITY_BUILDING`; governance status is `FROZEN`; future work prioritizes runtime, working UI, AI Draft Preview, Action Server, Email Runtime, and Inventory Runtime over documentation expansion. Governance expansion is forbidden except for governance bugs, safety issues, explicit Liad approval, or capability-safety requirements |
| AI Draft Recommendation Preview runtime implemented | Commit `3a488e6 Add AI draft preview runtime`; added `/ai-drafts/preview/5806` and a `Preview AI Draft` entry point from Service Report `1e25bbb1` / report `5806`; the route shows read-only recommendation lines for Air Filter, Oil Filter, 3L SKR oil top-up, Labor + Service, and Technician Visit / Travel with approval flags, evidence coverage, historical match gaps, risks, and no-write/user-approval language; validation: focused TypeScript passed, `git diff --check` passed with CRLF warnings only, outside-sandbox route checks returned HTTP 200 for `/ai-drafts/preview/5806`, `/service-reports/1e25bbb1`, and `/ai-drafts`, and expected content/link checks passed; `npm.cmd run build` compiled app code then failed on the existing unrelated missing `playwright` dependency/type declarations; no DB writes, Prisma changes, schema/env/migration changes, imports, BusinessDocument creation, Maven/Invoice4U action, inventory action, email/customer-facing action, source-system change, or production action occurred; current blocker is `none`; completion is 57% |
| Maven Knowledge / Pricing Evidence Layer implemented | Commit `5bd445d Add pricing evidence to AI draft preview`; `/ai-drafts/preview/5806` now connects preview item suggestions to trusted read-only pricing evidence only, using ProductCatalog direct SKU/alias first, historical BusinessDocumentItems second, and Maven item history third. Each line shows pricing evidence or the safe empty state `No trusted pricing evidence from ProductCatalog, BusinessDocumentItems, or Maven item history`; missing or conflicting evidence keeps approval required and no price is invented. Validation: focused TypeScript passed, `git diff --check` passed with CRLF warnings only, outside-sandbox route checks returned HTTP 200 for `/ai-drafts/preview/5806`, `/service-reports/1e25bbb1`, and `/ai-drafts`, and expected content/link checks passed; no DB writes, Prisma changes, schema/env/migration changes, imports, BusinessDocument creation, Maven/Invoice4U action, inventory action, email/customer-facing action, source-system change, or production action occurred; current blocker is `none`; completion is 58% |
| AI Draft Approval to BusinessDocument Draft runtime implemented | Commit `d3f40a1 Add AI draft approval draft creation`; `/ai-drafts/preview/5806` now includes a protected Server Action approval panel that requires the exact `APPROVE DRAFT` phrase, requires explicit pricing override when trusted pricing evidence is missing, rechecks the preview on submit, blocks duplicate BusinessDocument drafts for the same ServiceReport, and creates only internal `BusinessDocument`, `BusinessDocumentItem`, and `BusinessDocumentLog` rows. BusinessDocument detail now shows item price-approval state and preserved pricing evidence. Validation: focused TypeScript passed, `git diff --check` passed with CRLF warnings only, GET route validation returned HTTP 200 for `/ai-drafts/preview/5806`, `/business-documents`, and `/service-reports/1e25bbb1`, and expected approval panel/no-Maven/pricing text checks passed; no validation POST was submitted, so no DB rows were created during validation; the runtime performs no Maven/Invoice4U action, no AutomationCommand creation, no email/customer-facing action, and no inventory deduction; current blocker is `none`; completion is 59% |
| BusinessDocument Review and Approval Page implemented | Commit `3d4a667 Enhance business document review page`; `/business-documents/[id]` now presents internal draft review before external action, including source ServiceReport link, all BusinessDocumentItems, pricing evidence per item, approval-required warnings, and explicit `Internal Draft`, `Not sent`, `No Maven action`, `No email/customer-facing action`, and `No inventory deduction` states. Validation: focused TypeScript passed, `git diff --check` passed with CRLF warnings only, read-only route validation returned HTTP 200 for `/business-documents` and `/ai-drafts/preview/5806`, and `/business-documents/NEXT-AI-DRAFT-5806` returned HTTP 404 because no draft row exists and no protected POST was submitted; no DB writes, Maven/Invoice4U action, AutomationCommand creation, email/customer-facing action, inventory deduction, schema/env/migration change, import, source-system change, or production action occurred; current blocker is `none`; completion is 60% |
| Create Maven Draft AutomationCommand gate implemented | Commit `5fafc36 Add Maven draft command gate`; `/business-documents/[id]` now shows a protected Maven draft command gate and command-status panel. The Server Action requires exact phrase `CREATE MAVEN COMMAND`, only allows BusinessDocument status `APPROVED` or `READY_TO_SEND`, blocks duplicate `CREATE_MAVEN_DRAFT` commands for the same BusinessDocument, blocks missing items and unresolved price approval, and creates only a pending internal `AutomationCommand` with idempotency metadata. Validation: focused TypeScript passed, `git diff --check` passed with CRLF warnings only, GET-only route validation outside the sandbox returned HTTP 200 for `/business-documents`, `/ai-drafts/preview/5806`, and `/automation-commands`, and `/business-documents/NEXT-AI-DRAFT-5806` returned expected HTTP 404 because no draft row exists and no protected POST was submitted; no validation POST was submitted, so no AutomationCommand row was created during validation; no Maven/Invoice4U call, email/customer-facing action, inventory deduction, BusinessDocument mutation, schema/env/migration change, import, source-system change, or production action occurred; current blocker is `none`; completion is 61% |
| AutomationCommand Detail and Queue Review implemented | Commit `e4bdfbc Enhance automation command review`; `/automation-commands` and `/automation-commands/[id]` now inspect pending command rows before execution, including command type/status, source BusinessDocument, source ServiceReport when linked, external target, payload summary, raw source summary, idempotency/duplicate-protection evidence, and explicit Not executed / No Maven/Invoice4U call / No email/customer-facing action / No inventory action boundaries. Validation: focused TypeScript passed, `git diff --check` passed with CRLF warnings only, GET-only route validation outside the sandbox returned HTTP 200 for `/automation-commands`, `/business-documents`, and `/ai-drafts/preview/5806`, `/automation-commands/not-a-real-command` returned expected HTTP 404, and content validation confirmed the rendered list contains read-only queue review and no-run-command boundary text; no command execution, DB write, Maven/Invoice4U action, customer-facing action, inventory action, schema/env/migration change, import, source-system change, or production action occurred; current blocker is `none`; completion is 62% |
| Wave 2 end-to-end staging smoke test completed | Commit `3d269fb Handle missing AI draft quantities`; ServiceReport `5806` / `1e25bbb1` was validated through AI Draft Preview, protected BusinessDocument draft creation, BusinessDocument review, protected Maven AutomationCommand creation, AutomationCommand list/detail review, and duplicate checks. Created BusinessDocument `NEXT-AI-DRAFT-5806` / `1d7f8500-1cb7-4d81-ad3a-b7d5d8b453eb`; created AutomationCommand `NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806` / `db12ee97-0960-4f85-bdd5-f9fa30780885`; idempotency key `maven-draft:1d7f8500-1cb7-4d81-ad3a-b7d5d8b453eb`. Initial draft submit exposed and fixed `draftStatus=invalid-quantity` for `Missing hours`, now mapped to a zero-quantity approval-required line. Duplicate draft and duplicate command attempts were blocked by existing-record/no-form states and row counts remained one each. A staging-only eligibility update set the smoke-test BusinessDocument and five items to approved/ready state to exercise the command gate; no Maven/Invoice4U call, command execution, email/customer-facing action, inventory action, source-system touch, production action, schema/env/migration change, or import occurred. Focused TypeScript passed, `git diff --check` passed with CRLF warnings only, and route validation returned HTTP 200 for `/ai-drafts/preview/5806`, `/business-documents/NEXT-AI-DRAFT-5806`, `/automation-commands`, `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806`, and `/service-reports/1e25bbb1`; completion remains 62% because this was validation plus a bug fix, not a new capability point |
| BusinessDocument Approval Workflow implemented | Commit `b475f13 Add business document approval workflow`; `/business-documents/[id]` now supports protected Server Action approval and return-to-review workflow. Approval requires exact phrase `APPROVE BUSINESS DOCUMENT`, blocks unresolved pricing or missing/zero quantities unless the explicit override checkbox is selected, updates only internal BusinessDocument status/approval fields, and writes a `BusinessDocumentLog` audit row. Return-to-review requires a reason and writes audit history. The page shows approval blockers, workflow forms, no-external-action boundary text, and status history. Validation: focused TypeScript passed, `git diff --check` passed with CRLF warnings only, and GET-only route validation returned HTTP 200 for `/business-documents/NEXT-AI-DRAFT-5806`, `/business-documents`, `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806`, and `/ai-drafts/preview/5806`; no validation POST was submitted, so no DB write occurred during validation. No Maven/Invoice4U call, automatic AutomationCommand creation, email/customer-facing action, inventory action, schema/env/migration change, import, source-system action, or production action occurred; completion is now 63% |
| BusinessDocument Approval Workflow POST Smoke Test completed | 2026-06-25 staging smoke test against `NEXT-AI-DRAFT-5806` / `1d7f8500-1cb7-4d81-ad3a-b7d5d8b453eb`: exact approval phrase without override was blocked by pricing/quantity issues and created no log; return-to-review with reason created log `e4e1ce1a-f327-4e26-b7ff-6ec9af844835`; exact phrase without override remained blocked after return; explicit override approval created log `006dd5bf-e832-42b5-91a4-01ba7d4386e5` and restored final state to `APPROVED` / `APPROVED`. Final log count is 3 and `CREATE_MAVEN_DRAFT` command count remains 1. Validation: focused TypeScript passed, `git diff --check` passed, and route validation returned HTTP 200 for `/business-documents/NEXT-AI-DRAFT-5806?approvalStatus=approved`, `/business-documents/NEXT-AI-DRAFT-5806`, `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806`, `/business-documents`, and `/ai-drafts/preview/5806`; no Maven/Invoice4U call, automatic AutomationCommand creation, email/customer-facing action, inventory action, schema/env/migration change, import, source-system action, or production action occurred; completion remains 63% because this was validation, not a new capability point |
| Maven Execution Adapter Dry Run implemented | Commit `1ff28a3 Add Maven draft dry-run adapter`; `/automation-commands/[id]` now has a protected `CREATE_MAVEN_DRAFT` dry-run Server Action requiring exact phrase `DRY RUN MAVEN COMMAND`. It reads the command payload, validates the linked BusinessDocument is `APPROVED`, validates customer/document/line item readiness, stores `rawSource.mavenDryRun` with the would-send Maven payload, and keeps external state uncompleted. Active command `NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806` produced `DRY_RUN_BLOCKED` because five lines have missing trusted prices and `Labor + Service` has zero quantity; command status stayed `PENDING`, `processedAt=null`, `completedAt=null`, and `externalStateChanged=false`. Validation: focused TypeScript passed, `git diff --check` passed with CRLF warnings only, and route validation returned HTTP 200 for `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806?dryRunStatus=dry-run-blocked`, `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806`, `/automation-commands`, and `/business-documents/NEXT-AI-DRAFT-5806`; no Maven/Invoice4U call, external document creation, email/customer-facing action, inventory action, schema/env/migration change, import, source-system action, or production action occurred; completion is now 64% |
| BusinessDocument Line Resolution Layer implemented | Commit `8538455 Add business document line resolution layer`; `/business-documents/[id]` now supports protected internal correction of BusinessDocument draft line blockers. The Server Action updates only quantity, unit price, total price, pricing evidence in `rawSource`, and the approval-required flag, and writes `BusinessDocumentLog` audit history. Validation: focused TypeScript passed, `git diff --check` passed with CRLF warnings only, and GET route validation returned HTTP 200 for `/business-documents/NEXT-AI-DRAFT-5806`, `/business-documents/NEXT-AI-DRAFT-5806?lineStatus=line-saved`, and `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806`; no validation POST was submitted, so no DB row changed during validation. No Maven/Invoice4U call, AutomationCommand execution, email/customer-facing action, inventory action, schema/env/migration change, import, source-system action, or production action occurred; completion is now 65% |
| Wave 2 Line Resolution POST Smoke Test completed | 2026-06-25 staging smoke test against `NEXT-AI-DRAFT-5806` / `1d7f8500-1cb7-4d81-ad3a-b7d5d8b453eb`: five BusinessDocument line corrections were saved with test/manual pricing evidence and one `BusinessDocumentLog` per line: `a3c0f56b-bb70-4c85-999d-30b26075ac67`, `7a8a2933-a591-441b-ab11-fafbf0bae1db`, `534763b1-29b5-4258-9214-0121d51e32da`, `66ecc9ec-e2cd-4d0c-bb7e-7ff26313ad9c`, and `1f76221b-0f9c-4039-a096-699b7da314a9`. Final lines: Air Filter `3 x 120 = 360`; Technician Visit / Travel `1 x 300 = 300`; Oil Filter `3 x 90 = 270`; 3L SKR oil top-up `9 x 45 = 405`; Labor + Service `2 x 275 = 550`; all have `needsPriceApproval=false`. Recalculated blocker count is `0`. Maven dry-run was rerun only after fixes and produced `DRY_RUN_VALIDATED`, blocker count `0`, warnings `0`, command status `PENDING`, `processedAt=null`, and `completedAt=null`. Focused TypeScript passed; `git diff --check` passed; route validation outside the sandbox returned HTTP 200 for `/business-documents/NEXT-AI-DRAFT-5806`, `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806`, and `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806?dryRunStatus=dry-run-validated`; no Maven/Invoice4U call, command execution, email/customer-facing action, inventory action, schema/env/migration change, import, source-system action, or production action occurred; completion remains 65% because this was validation, not a new capability point |
| Wave 2 closed and Maven execution readiness checklist prepared | ServiceReport `5806` chain is closed from AI Draft Preview through final Maven dry-run validation. Created IDs and final values are recorded in `project-brain/CURRENT_TASK.md` and `project-brain/current/LIVE_OBJECTS.md`; final dry-run payload status is `DRY_RUN_VALIDATED`, blocker count `0`, command status `PENDING`, `processedAt=null`, and `completedAt=null`. Boundaries confirmed: no Maven/Invoice4U call, no AutomationCommand execution, no external document creation, no email/customer-facing action, no inventory deduction, no schema/env/migration/import/source-system/production action. Wave 2 is complete at `15% / 15%`; project completion remains 65%. The real Maven execution gate is documented as `APPROVAL_REQUIRED` with pre-execution dry-run, payload review, executor ownership, idempotency, target environment, post-execution update, failure handling, email separation, inventory separation, and rollback requirements |
| Wave 2 Architecture Audit completed | Documentation-only audit reviewed every Wave 2 runtime against the original migration plan and current runtime scope. Result: runtime chain is safe for approved ServiceReport `5806` scope, Server Actions are used for internal writes, route structure is consistent, and AI Draft / BusinessDocument / AutomationCommand responsibilities are mostly clean. Debt recorded in `project-brain/CURRENT_TASK.md`: report-5806-specific recommendation logic, inline pricing evidence selection, duplicated UUID/JSON/decimal/blocker helpers, hard-coded approval phrases/default operator/fixed staging values, implicit JSON evidence contracts, and inline Maven dry-run validation/payload building. Safe-to-start decision: Wave 3 may start as read-only Maven data discovery/import planning only; real Maven execution remains separately gated. No runtime behavior changed and no DB writes occurred |
| Wave 3 Start: Maven Knowledge Layer Read-Only Discovery completed | Wave 3 was started safely as planning/discovery only. `project-brain/CURRENT_TASK.md` now records the Wave 3 plan, BusinessDocument-to-Maven payload mapping, required Maven fields, missing data gaps, Maven payload-builder extraction plan, and real Maven execution readiness checklist. Key gap: checked Apps Script source shows legacy `createMavenDraft(data)` logging/updating `BusinessDocuments` status, but does not prove a real Maven create-draft API request contract. No runtime behavior changed, no DB writes occurred, no Maven/Invoice4U execution occurred, no email/customer-facing action occurred, no inventory action occurred, and no production/source-system changes occurred |
| Maven dry-run payload builder extracted | `lib/maven-draft-payload.ts` now owns `buildMavenDraftPayload`, `validateMavenDraftPayload`, and `MavenDraftBlockersAndWarnings`; `app/automation-commands/[id]/actions.ts` keeps the Server Action gate, command lookup, internal dry-run update, revalidation, and redirects. `scripts/validate-maven-draft-payload.ts` provides a focused compile-time fixture for the extracted builder/validator. Validation: focused TypeScript passed for touched files and Wave 2 runtime files; `git diff --check` passed with CRLF warnings only; route validation returned HTTP 200 for `/ai-drafts/preview/5806`, `/business-documents/NEXT-AI-DRAFT-5806`, `/automation-commands/NEXT-MAVEN-CMD-NEXT-AI-DRAFT-5806?dryRunStatus=dry-run-validated`, and `/automation-commands`. No real Maven/Invoice4U call, external execution, DB write, email/customer-facing action, inventory action, source-system change, or production change occurred |
| Wave 3 Read-Only Maven Source Inventory completed | Commit `5b6c44b Document Maven source inventory`; `project-brain/migration/MAVEN_SOURCE_INVENTORY.md` documents Maven-related source objects: `InvoiceMavenDocuments`, `InvoiceMavenDocumentItems`, `InvoiceMavenCustomers`, `InvoiceMavenItems`, `SyncState`, `SyncLog`, `ErrorLog`, `BusinessDocuments`, `BusinessDocumentItems`, `BusinessDocumentLog`, `AutomationCommands`, selected `ServiceReports` Maven flags, `Lists`, `AutomationRegistry`, `AppMenu`, Apps Script properties, and Maven-related Apps Script functions. The inventory includes purpose, keys, relationships, read/write direction, fields, required/optional fields, usage, dependencies, relationship diagram, data flow, migration recommendations, and unknowns. Key finding: checked local source proves Maven document search/import but does not prove a real Maven draft-create API contract. Validation: `git diff --check` passed with CRLF warnings only; TypeScript/route validation was not run because no runtime files changed. No runtime behavior changed, no DB write occurred, no Maven/Invoice4U execution occurred, no email/customer-facing action occurred, no inventory action occurred, and no source-system/production change occurred |
| Maven API environment placeholders prepared | `.env.staging.example` now contains blank Maven API placeholder names only, including `MAVEN_API_BASE_URL`, `MAVEN_API_KEY`, endpoint placeholders, target environment, timeout, and `MAVEN_EXECUTION_ENABLED=false`. `project-brain/migration/MAVEN_SOURCE_INVENTORY.md` documents required Maven API values and the pre-execution checklist. No real secrets were committed. No code reads these variables yet. No Maven/Invoice4U call, executor implementation, runtime behavior change, DB write, schema/env secret change, import, email/customer-facing action, inventory action, source-system action, or production action occurred |
| Internal Business Document and Payment Engine implemented | Commit `4b87e1b Add internal business document payment engine`; `lib/business-document-engine.ts` validates internal BusinessDocument document type support, VAT/totals/payment amount/balance due, receipt-payment requirements, export blockers, warnings, supported payment sources, and future check/bank-proof attachment readiness. `/business-documents/[id]` renders the engine review panel. Focused TypeScript passed for touched files; repo-wide TypeScript still has pre-existing unrelated AI Draft and Playwright errors; `git diff --check` passed with CRLF warnings only; route validation returned HTTP 200 for `/business-documents` and `/business-documents/NEXT-AI-DRAFT-5806` and confirmed the new engine panel, document types, payment sources, balance due, and blocked-export boundary. No DB write, schema change, Maven/Invoice4U call, real export implementation, email/customer action, inventory deduction, or attachment upload/storage implementation occurred. Completion is now 66% |
| Internal BusinessDocument HTML Preview implemented | Commit `368cd1b Add business document HTML preview`; `/business-documents/[id]/preview` renders a read-only Hebrew RTL HTML preview based on the Maven sample PDF structure, using the existing BusinessDocument adapter and engine output. The BusinessDocument review page links to the preview. Validation: focused TypeScript passed; `git diff --check` passed with CRLF warnings only; route validation returned HTTP 200 for `/business-documents/NEXT-AI-DRAFT-5806` and `/business-documents/NEXT-AI-DRAFT-5806/preview`, and content checks confirmed company/logo text, document number, source ServiceReport, item table content, subtotal, VAT, balance due, digital signature area, and no-Maven boundary. No PDF generation, DB write, schema change, Maven/Invoice4U call, real export implementation, email/customer action, inventory deduction, source-system action, or production action occurred. Completion is now 67% |
| BusinessDocument HTML Preview accuracy pass implemented | Commit `0ff5720 Refine business document HTML preview`; `/business-documents/[id]/preview` received an HTML/CSS-only accuracy pass: tighter print-document proportions, metadata strip, RTL table alignment, fixed item/totals columns, stronger totals emphasis, signature line, no-external-action boundary strip, and A4 print CSS. Local search did not find `document_102488.pdf` or a safe Tal logo asset, so direct PDF rendering comparison and logo replacement remain gaps. Validation: focused TypeScript passed; repo-wide TypeScript still fails on pre-existing unrelated AI Draft pricing-evidence typing and missing Playwright dependency/type issues; `git diff --check` passed with CRLF warnings only; route validation returned HTTP 200 for `/business-documents/NEXT-AI-DRAFT-5806/preview` and `/business-documents/NEXT-AI-DRAFT-5806`; no PDF generation, DB write, schema change, Maven/Invoice4U call, external API call, email/customer action, inventory deduction, source-system action, or production action occurred. Completion remains 67% |
| Maven sample asset imported | Commit `f1dcd47 Add Maven sample reference PDF`; uploaded sample copied to `project-brain/reference/maven-samples/document_102488.pdf` as reference-only artifact, observed size `61973` bytes. Preview route `/business-documents/NEXT-AI-DRAFT-5806/preview` revalidated with HTTP 200 and expected preview boundary/metadata/totals/signature content. Direct rendered visual comparison could not be completed locally because Poppler/Ghostscript/ImageMagick are unavailable, `sharp` cannot rasterize the PDF, Python is unavailable as a working runtime, and headless Chrome did not emit screenshot files. No runtime behavior changed; no PDF generation, Maven/Invoice4U call, external API call, email/customer action, inventory action, DB write, schema change, source-system action, or production action occurred. Completion remains 67% |
| Internal PDF Export Planning completed | Commit `df0c144 Plan internal PDF export path`; recommended implementation path is server-side Playwright/Chromium `page.pdf()` against `/business-documents/[id]/preview`, exposed first as temporary-download-only `GET /business-documents/[id]/pdf`. Plan records dependency needs, Hebrew/RTL/font/page-size risks, temporary vs saved-file storage strategy, route/action design, and validation steps against `project-brain/reference/maven-samples/document_102488.pdf`. No runtime behavior changed, no PDF generation occurred, no dependency was installed, no Maven/Invoice4U call, external API call, email/customer action, inventory action, DB write, schema change, source-system action, or production action occurred. Completion remains 67% |
| Internal PDF Export MVP implemented | Commit `1277121 Add internal business document PDF export`; `GET /business-documents/[id]/pdf` renders the existing internal preview route with Playwright/Chromium and streams an A4 `application/pdf` attachment as a temporary download. Review and preview pages link to the route. Validation confirmed HTTP 200, `application/pdf`, attachment filename `NEXT-AI-DRAFT-5806-internal-preview.pdf`, `%PDF-` signature, and 82096 bytes. No Maven/Invoice4U call, external API call, email/customer action, inventory action, DB write, schema change, file persistence, sent/exported status mutation, source-system action, or production action occurred. Completion is now 68% |
| PDF Export Smoke Test and Governance Lock completed | Commit `PENDING - PDF smoke governance commit`; Chrome opened the generated `/business-documents/NEXT-AI-DRAFT-5806/pdf` attachment. Page 1 and page 2 rendered in Chrome's PDF viewer with readable Hebrew/RTL layout. Smoke test found stale stored header totals, so `lib/business-document-engine.ts` now uses current line totals for review/PDF display when stored totals are missing or stale. Validated totals: subtotal `1885.00 ILS`, VAT `320.45 ILS` at 17%, total `2205.45 ILS`, balance due `2205.45 ILS`. DB read confirmed `status=APPROVED`, `sendStatus=null`, `mavenPdfLink=null`, `mavenDocumentNumber=null`; app route persisted no file and temporary smoke-test files were removed. npm audit findings documented only: direct `next` critical aggregate with fix available to `15.5.19`, transitive `postcss` moderate via `next`; no automatic fix was run. Governance lock added: Codex must not install new npm packages without explicit approval per package. Minor remaining layout gap: a horizontal overlap line crosses the subtotal row in the PDF viewer. No Maven/Invoice4U call, external API call, email/customer action, inventory action, DB write, schema change, saved file, sent/exported status mutation, source-system action, or production action occurred |
| PDF Visual Polish Bugfix completed | Commit `737245c Polish business document PDF totals layout`; `app/globals.css` print rules now prevent the line table and totals block from colliding in the generated PDF. Validation: focused TypeScript passed; preview route returned HTTP 200; PDF route returned HTTP 200 with `application/pdf`; totals remained `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; temporary Chrome PDF viewer render confirmed the subtotal row is no longer crossed by a horizontal line. No DB write, Maven/Invoice4U call, email/customer action, inventory action, schema change, totals logic change, saved file, sent/exported status mutation, source-system action, or production action occurred |
| Hebrew Customer-Facing PDF Layout Fix completed | Commit `b93c303 Make business document PDF Hebrew customer facing`; `/business-documents/[id]/preview` and `/business-documents/[id]/pdf` now render a Hebrew RTL customer-facing document surface. Internal staging notes, evidence text, source/debug fields, boundary text, unsupported/legacy labels, and Maven references are hidden from preview/PDF. PDF renders as one readable A4 page in Chrome PDF viewer. Validation: focused TypeScript passed; preview route HTTP 200; PDF route HTTP 200 with `application/pdf`; totals remained `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; PDF filename is `NEXT-AI-DRAFT-5806.pdf`. No DB write, Maven/Invoice4U call, email/customer action, inventory action, schema change, totals logic change, saved file, sent/exported status mutation, source-system action, or production action occurred |
| Manufacturer SKU Excel Source Planning completed | Commit `95e99d1 Plan manufacturer SKU Excel sources`; existing SKU specs now document the current manufacturer Excel source inventory, PM workbook sheets/columns/model structure, EPM legacy-binary parser gap, future SKU registry schema plan, matching rules, audit/source evidence fields, gradual import/enrichment plan, and customer-facing PDF SKU display rule. No schema change, DB write, import, Maven/Invoice4U call, email/customer action, inventory action, source-system action, or production action occurred |
| ServiceReport 5806 SKU Matching Runtime MVP completed | Commit `cac31e4 Add runtime SKU matching for report 5806`; BusinessDocument review now enriches ServiceReport `5806` lines with trusted PM Series 40PM manufacturer SKU evidence at read/runtime only. Internal review shows SKU match status, manufacturer, confidence, part category, Excel file/sheet/row, compatible models, reason, and needs-review flag. Customer preview/PDF shows Hebrew `מק"ט` column only when a trusted match exists. Validated trusted SKUs on `NEXT-AI-DRAFT-5806`: Air Filter `25100043-071`, Oil Filter `25200007-005`, and coolant/oil `80000175-039`; unmatched lines show review-required internally and stay blank on preview/PDF. Focused TypeScript passed; review and preview routes returned HTTP 200; PDF route returned HTTP 200 with `application/pdf`; totals remained `1885.00 ILS`, `320.45 ILS`, and `2205.45 ILS`; `git diff --check` passed with CRLF warnings only. No schema change, SKU import, DB write, Maven/Invoice4U call, email/customer action, inventory action, source-system action, production action, or price change occurred. Completion moves to 69% |

## BLOCKED / NOT STARTED

| Task | Blocker | Next Unblock Step |
|---|---|---|
| Database migration | Migrations are not part of current staging shadow path | Separate approval required before any Prisma migrate workflow |
| Additional schema push | Staging schema is already applied; further schema changes need separate approval | Approve any schema change separately before another db push |
| Wave 2 import | Wave 2 import is not approved and dry-run import readiness is FAIL/BLOCKED | Request explicit import approval only after blockers are resolved or explicitly accepted |
| Production integration | Shadow app is not approved for production | Keep AppSheet/Sheets production until explicit cutover approval |
| Real Maven execution | Wave 2 dry-run readiness is validated, but real Maven/Invoice4U execution is not approved | Use the Maven Execution Readiness Checklist in `project-brain/CURRENT_TASK.md`; Liad must explicitly approve real execution, executor ownership, target environment, final payload, allowed post-execution writes, and rollback/failure handling before any Maven call or command execution |
| Maven document-generation API contract | Current checked Apps Script source does not contain a proven real Maven document-generation endpoint/request/response schema | Obtain primary Maven/API evidence for document-generation endpoint, auth, request fields, response fields, errors, and rate limits before any executor implementation |
| Maven API real secret placement | Placeholder names exist, but real values are not approved and must not be committed | Approve target environment and secret store before placing real Maven API values |
| Payment attachment storage | Uploaded check image / bank proof attachment is future-ready only and has no approved storage/review/retention policy | Approve storage target, privacy rules, evidence review workflow, retention policy, and access controls before implementation |
| Broad AI Draft rollout beyond ServiceReport 5806 | Wave 2 Architecture Audit found report-specific recommendation logic and inline pricing evidence selection | Generalize service-pattern rules, pricing evidence, and readiness validation before using AI Draft Preview broadly |
| New planning/control files | Existing-file audit is required first | Search existing files and prove no owner file already exists |

## COMPLETION / READINESS SPLIT

| Area | Current State |
|---|---|
| Infrastructure readiness | High for staging, Prisma, Wave 1 import, and read-only Supabase validation evidence |
| Workflow UI coverage | Wave 2 BusinessCase and Service Workflow Layer is complete and frozen except bug fixes. Wave 3 Commercial Runtime and Document Engine has BusinessCase Runtime Sprint 1, Commercial Lifecycle Hardening Sprint 2, Operations Center Sprint 4, Operations Command Center Sprint 5, Customer 360 Workspace Sprint 6, End-to-End Service Flow MVP Sprint 7, Generalized BusinessDocument Draft Gateway Sprint 8, Business Intent Policy Layer Sprint 9, Production Draft Generation Sprint 10, Production Draft Review and Learning Loop Sprint 11, Autonomous Business Draft Generation Sprint 12, internal BusinessDocument/payment review, internal HTML preview, temporary PDF download, smoke-tested line-derived totals, ServiceReport `5806` runtime manufacturer SKU matching, generated Manufacturer Parts Registry fixtures, service-kit intelligence, shared SKU overlap evidence, domain-boundary helpers, and supporting read-only discovery artifacts. Wave 4 Financial Runtime and Settlement has started with internal Financial Intake, matching, approval review, receipt draft, tax invoice / receipt draft, and BusinessCase financial status. Maven is an External Adapter readiness concern, not the architectural center. Current evidence-based completion is 81% by the corrected transitional capability formula |
| Production automation readiness | Gated; Wave 2 produced an inspected pending Maven command and `DRY_RUN_VALIDATED` payload, but real Maven/Invoice4U execution, command execution, email/customer-facing action, inventory action, production integrations, schema changes, migrations, and DB writes outside approved protected flows require explicit human approval |

## Rules

- No production writes.
- Project Mode is `CAPABILITY_BUILDING`.
- Governance Status is `FROZEN`.
- Current Priority is working runtime capabilities instead of documentation expansion.
- New specs, registries, knowledge bases, governance documents, roadmap items, or decision systems are forbidden unless a governance bug, safety issue, explicit Liad approval, or capability-safety requirement exists.
- Every proposed task must answer what new capability will exist after completion. If no new capability will exist, stop and recommend merge, reuse, extend, or reject.
- Autonomous Work Loop is active for safe tasks.
- TDOS risk-based operating model is active inside `PROJECT_OPERATING_PROTOCOL.md`; no required control for the task's risk class may be skipped.
- Startup reads must include canonical startup sources and all sources relevant to affected business objects, workflows, agents, routes, schema, and approval gates.
- Codex is the main Orchestrator and Project Executive; every task must pass through the Orchestrator Decision Engine before work begins.
- Every task must run the Executive Cycle: Understand, Discover, Consult, Score, Decide, Execute, Validate, Learn, Improve.
- Before choosing a solution, Codex must consult relevant existing agents and summarize recommendation, risks, evidence, confidence, and better alternatives.
- Every candidate task or path must be scored for Business Value, Technical Value, Project Acceleration, Reuse Score, Duplicate Risk, Runtime Impact, Long-term Value, Complexity, and Estimated Time.
- Reuse before create is mandatory: do not create a new Agent, Registry, Spec, Roadmap item, or Knowledge Base until existing assets have been searched, verified, and rejected as insufficient.
- If an artifact repeats another artifact, it must be deleted, generated, or reduced to a pointer.
- Manual artifacts must have owner/source/lifecycle; generated artifacts must declare generator/sources/validation.
- Creating an Agent, Registry, Spec, Rule, Knowledge Base, or Roadmap Item without already-searched/already-verified/already-rejected proof is `FORBIDDEN`.
- Project progress is measured by new capabilities, not by number of new documents. If the output is only a new document, first check whether it can be merged, appended to an existing document, or converted into an actual capability.
- Reality Checks should report duplicate work prevented, reuse percentage, capabilities added, documentation created, capability/documentation ratio, project acceleration score, outstanding executive decisions, and highest-value next task.
- Orchestrator authority levels are `AUTO_EXECUTE`, `REPORT_ONLY`, `APPROVAL_REQUIRED`, and `FORBIDDEN`.
- Codex must route work to existing agent owners by role.
- Codex must not ask Liad for every small step; it must work, validate, collect proof, update Project Brain, and stop only at meaningful APPROVAL_REQUIRED gates.
- AUTO_ALLOWED work may proceed without routine confirmation when it is the next approved task: repo inspection, local tests/type checks, read-only DB queries, read-only UI validation, documentation updates, read-only UI/display mapping fixes, local validation reports, Project Brain updates after completed safe work, and safe/scoped commit-push after validation.
- AUTO_APPROVED actions must run without asking Liad for approval:
  - Git: `git fetch`, `git pull --ff-only`, `git status`, `git log`, `git branch -vv`.
  - Read-only validation: read-only validation, read-only database queries, read-only DB query, Prisma read-only queries, staging read-only verification, count validation, relationship validation.
  - Local development: local tests, TypeScript compile checks, Next.js build checks, validation against the user-owned persistent Next.js dev server at `http://localhost:3000`, local HTTP validation, Playwright read-only validation, screenshot generation for proof, HTML render validation, route validation.
- Dev server ownership rule: Codex must not start or own the Next.js development server by default; assume Liad keeps one persistent local terminal running `npm run dev`; use `http://localhost:3000` for validation whenever possible; start/restart only when explicitly requested or when Liad reports it is no longer running; do not leave long-running dev processes attached to Codex sessions.
  - Project Brain: update Project Brain after completed safe work, update `project-brain/CURRENT_TASK.md`, update `project-brain/TASK_BOARD.md`, update `project-brain/DECISION_LOG.md`, update `PROJECT_INDEX.md` references, update migration plans, update wave progress.
  - Safe commits: documentation-only commits, Project Brain commits, governance commits, read-only validation report commits, safe implementation commits after validation, `git push` of approved safe-scope work.
- Do not ask Liad for approval when executing AUTO_APPROVED actions; only stop for APPROVAL_REQUIRED gates.
- APPROVAL_REQUIRED work must stop for explicit Liad approval: installing any new npm package with approval naming each package, `schema.prisma` changes, Prisma `db push`, Prisma `migrate`, DB writes, imports, seeds, Supabase project/settings changes, Google Sheets changes, AppSheet changes, Maven changes, Apps Script changes, Drive writes, email/customer-facing actions, production deployment, production cutover, deleting business data, deleting source files, new agent architecture, and new governance architecture.
- Before stopping for approval, Codex must verify whether the action is AUTO_APPROVED. If AUTO_APPROVED, continue working. If APPROVAL_REQUIRED, stop and present an Executive Approval Report.
- Executive Approval Reports must start with `EXECUTIVE APPROVAL REQUEST` and include PROJECT POSITION, REQUEST, WHY, EVIDENCE, FILES TO CHANGE, SYSTEMS TOUCHED, SYSTEMS CONFIRMED UNTOUCHED, RISK, ROLLBACK, AFTER APPROVAL, and DECISION REQUIRED.
- Executive Approval Reports must fit the executive summary within about 15 lines before detailed evidence, must not present raw logs first, must include Project Tree Position and PROJECT COMPLETION MODEL, must include Risk, Rollback, and Systems Confirmed Untouched, and must stop for Liad to Approve, Reject, or Modify.
- Executive Approval Reports must include IMPACT ANALYSIS. No approval request is valid without Impact Analysis.
- IMPACT ANALYSIS must report systems affected, systems verified unaffected, Regression Review, Dependency Review, Approval Confidence, and the mandatory statement.
- Systems affected must list exact files, modules, routes, tables, and agents.
- Systems verified unaffected must cover Project Brain, Governance, Wave 1, existing imports, existing Prisma schema, existing Supabase data, existing AppSheet logic, existing Maven integrations, existing automation flows, and existing inventory logic.
- Approval Confidence must be LOW, MEDIUM, or HIGH.
- IMPACT ANALYSIS must explicitly state: "I checked for impact on existing project logic and future approved project roadmap."
- Approval-gate reports must include what was done, what was checked, proof, risks, requested approval, what happens after approval, and systems confirmed untouched.
- Proof Requirement: before closing any completed task, Codex must provide what was wrong before, what changed, evidence, validation result, and user-visible impact.
- Preferred evidence includes screenshots, Playwright screenshots, HTML render samples, before/after comparisons, and counts.
- A task is not complete with only HTTP 200, PASS, or compile success; Codex must demonstrate the visible outcome whenever possible.
- Mandatory Project Tree Reporting: every Reality Check, Approval Gate, Autonomous Completion Report, and `by codex` closeout must include PROJECT TREE, CURRENT POSITION, PROJECT COMPLETION MODEL, CRITICAL PATH, and NEXT APPROVAL GATE using the canonical project tree from `PROJECT_INDEX.md`.
- PROJECT COMPLETION MODEL must be evidence-based and capability-weighted, not completed waves divided by total waves. Current transitional capability weights: Governance / Project Brain / Git workflow 15%; Supabase + Prisma Data Layer 15%; Import Framework + Wave 1 Import 10%; Wave 1 Party, Asset, and Service Operations UI 10%; Wave 2 BusinessCase and Service Workflow Layer 15%; Wave 3 Commercial Runtime and Document Engine 15%; Wave 4 Financial Runtime and Settlement 10%; Wave 5 Inventory and Procurement 5%; Wave 6 Automation and Integration Adapters 3%; Wave 7-9 Offline Field Runtime / Production Shadow / Cutover / AppSheet Retirement 2%. A later approved scoring pass should replace these transitional wave labels with a domain capability scorecard.
- A task is not considered complete unless Project Tree Position is reported.
- Proof Requirement and Project Tree Position are both mandatory.
- Documentation/state sync only for current Project Brain update unless Liad explicitly approves script changes.
- No additional code implementation.
- No Prisma commands.
- No database creation or migration.
- No schema push.
- No additional import.
- No production cutover.
- No Google Sheets.
- No AppSheet.
- No Maven.
- No VPS provisioning or remote infrastructure setup.
- No Wave 2/3/4 import work until Liad explicitly approves that later gate.
- Every task must have an owner agent or explicit `manual/Liad approval` owner before work starts.
