# Application Route Map

Last updated: 2026-06-28

Update note - SKU Runtime Generalization Refactor:

- `/business-documents/[id]` now shows read/runtime manufacturer SKU match status through the generic ServiceReport -> Equipment model -> Manufacturer Parts Registry -> SKU Matching Engine flow.
- ServiceReport `5806` and PM `40PM` rows remain validation/sample registry evidence only, not runtime architecture gates.
- Internal review shows Tal sales SKU, manufacturer SKU, manufacturer, confidence, source PM Series Excel file, sheet, row, compatible models, needs-review state, and `Needs sales SKU mapping` when no sales SKU exists.
- `/business-documents/[id]/preview` and `/business-documents/[id]/pdf` show a Hebrew SKU column only when a linked Tal sales SKU exists. Manufacturer part numbers remain internal-only and are hidden from the customer-facing surface.
- This route behavior is read-only enrichment from existing BusinessDocument and ServiceReport reads. No SKU import, schema change, DB write, Maven/Invoice4U call, email/customer action, inventory action, or source-system action is part of the route update.

Scope: currently implemented Next.js `app/**/page.tsx` routes plus internal route handlers that provide user-visible runtime capability.

Record counts were verified with read-only Prisma count queries against the current Supabase staging database.

| Route | Module | Status | Data Source | Record Count | AppSheet Equivalent |
|---|---|---|---|---:|---|
| `/` | Dashboard | Implemented | Static Next.js module registry in `app/page.tsx` | N/A | AppSheet app home / navigation |
| `/operations` | TAL Operations Command Center | Implemented, read-only action-driven operations workspace; projects existing BusinessCases into My Work Today, Immediate Attention, High Priority, Waiting For Technician, Waiting For Customer, Waiting For Approval, Waiting For Financial Action, Waiting For Parts, and Ready To Close. Rows show customer, asset, BusinessCase, current stage, blocker, next recommended action, priority, assigned owner, last activity, commercial status, financial status, and an internal open-action link. Recommendations are advisory only and do not execute actions | Existing BusinessCase Runtime via ServiceReport, BusinessDocument, Approval, AutomationCommand, Commercial lifecycle, Financial Intake runtime, and read-only Inventory impact status; queue rows are not persisted tasks; no Prisma model, schema change, DB write, external action, Maven/Invoice4U call, email/customer action, inventory mutation, cloud action, or production behavior | 63 service-report-derived cases addressable through existing BusinessCase runtime | Daily AppSheet home / operations work-queue replacement candidate |
| `/service-reports` | Service Reports list | Implemented, read-only | Supabase PostgreSQL via Prisma `ServiceReport` | 63 | `ServiceReports` table list view |
| `/service-reports/[id]` | Service Report detail | Implemented, read-only | Supabase PostgreSQL via Prisma `ServiceReport` with linked customer/equipment context | 63 addressable reports | `ServiceReports` detail view |
| `/business-cases/service-report/[id]` | BusinessCase operational context | Implemented application/runtime concept derived from an existing ServiceReport; orchestrates Party, Asset, ServiceReport, AI Recommendation, BusinessDocument, Approval, AutomationCommand, Financial Intake, Inventory Impact, blockers, closure readiness, timeline, and End-to-End Service Flow. Sprint 7 added Business Intelligence Analysis, Business Suggestions, flow steps through Operations Command Center and Customer 360, and Approval Ready context. Sprint 8 added the protected BusinessDocument Draft Gateway. Sprint 9 adds Business Intent Policy above the gateway: BusinessCase -> Business Intelligence -> Business Intent -> Domain Policy -> Allowed Draft Type -> Gateway. Initial intents are `PROPOSE_WORK`, `BILL_CUSTOMER`, `COLLECT_PAYMENT`, `CREDIT_CUSTOMER`, `PURCHASE_PARTS`, `DELIVER_GOODS`, `WARRANTY_REVIEW`, and `SCHEDULE_FOLLOW_UP`; only `PROPOSE_WORK -> QUOTE` and `BILL_CUSTOMER -> INVOICE` can create drafts now | Existing adapters and Supabase PostgreSQL reads through ServiceReport, BusinessDocument, AutomationCommand, Commercial lifecycle, Financial Intake, Approval, Automation, Operations Command Center, and Customer 360 runtime; Sprint 8/9 POST writes only internal `BusinessDocument`, `BusinessDocumentItem`, and `BusinessDocumentLog` rows through the gateway after policy approval. No Prisma model, schema change, Maven/Invoice4U call, email/customer action, inventory mutation, OCR, bank API, receipt issuing, cloud action, or production behavior | 63 service-report-derived cases addressable by source ServiceReport ID; gateway validation created `NEXT-BD-DRAFT-9981b978-QUOTE` and `NEXT-BD-DRAFT-9981b978-INVOICE` from non-5806 ServiceReport `5802` | Operational case context above existing AppSheet service/report/document workflow |
| `/customers` | Customers list | Implemented, read-only | Supabase PostgreSQL via Prisma `Customer` | 763 | `Customers_Final` / Customers list view |
| `/customers/[id]` | Customer 360 Workspace | Implemented, read-only Customer 360 projection answering what is happening with the customer right now. Composes Customer Summary, Assets, Open Business Cases, Open Commercial Documents, Financial Status, Current Blockers, Recommended Next Action, Recent Timeline, and Future Opportunities from existing runtime. This is not a CRM engine and does not duplicate BusinessCase or Operations Command Center | Supabase PostgreSQL via Prisma `Customer` plus existing BusinessCase Runtime, ServiceReport, BusinessDocument, Approval, AutomationCommand, Commercial lifecycle, Financial Intake runtime, and read-only Inventory impact status; no Prisma model, schema change, DB write, CRM workflow, external action, Maven/Invoice4U call, email/customer action, inventory mutation, cloud action, or production behavior | 763 addressable customers | Customer 360 / customer relationship context |
| `/equipment` | Equipment list | Implemented, read-only | Supabase PostgreSQL via Prisma `ReportEquipmentItem` | 75 | `ReportEquipmentItems` / equipment inline or related view |
| `/equipment/[id]` | Equipment detail | Implemented, read-only | Supabase PostgreSQL via Prisma `ReportEquipmentItem` with linked service-report context | 75 addressable equipment rows | `ReportEquipmentItems` detail/related record view |
| `/inventory-stock` | Inventory / equipment stock redirect | Implemented route alias | Redirects to existing `/equipment` route; no separate data module | N/A | App navigation alias to equipment/stock area |
| `/parts-used` | Parts Used list | Implemented, read-only | Supabase PostgreSQL via Prisma `PartUsed` | 0 | `PartsUsed` table list view |
| `/parts-used/[id]` | Parts Used detail | Implemented, read-only | Supabase PostgreSQL via Prisma `PartUsed` with linked service-report/product context when present | 0 addressable parts-used rows | `PartsUsed` detail view |
| `/ai-drafts` | AI Draft Suggestions list | Implemented, read-only empty-state shell | Supabase PostgreSQL via Prisma `AiDraftSuggestion` | 0 | `AIDraftSuggestions` table list view |
| `/ai-drafts/[id]` | AI Draft Suggestion detail | Implemented, read-only empty-state shell | Supabase PostgreSQL via Prisma `AiDraftSuggestion` with linked customer/service-report context when present | 0 addressable AI draft rows | `AIDraftSuggestions` detail view |
| `/ai-drafts/preview/[reportCounter]` | AI Draft Recommendation Preview | Implemented runtime preview for Service Report `5806`; GET view is read-only, line pricing shows trusted evidence only, and protected Server Action can create one internal BusinessDocument draft only after explicit approval phrase and pricing override when required | Supabase PostgreSQL via Prisma `ServiceReport`, `ReportEquipmentItem`, `PartsUsed`, `Product`, `MavenDocumentItem`, `BusinessDocument`, `BusinessDocumentItem`, `BusinessDocumentLog`, and `AiDraftSuggestion`; POST action writes internal `BusinessDocument`, `BusinessDocumentItem`, and audit log rows only | 1 validated preview route | AI Draft recommendation review and approval-to-internal-draft gate |
| `/business-documents` | Business Documents list | Implemented, read-only empty-state shell | Supabase PostgreSQL via Prisma `BusinessDocument` | 0 | `BusinessDocuments` table list view |
| `/business-documents/[id]` | BusinessDocument Review and Approval Page | Implemented internal draft review page with protected BusinessDocument approval/rejection workflow, protected line-resolution layer, and protected Maven draft command gate; GET shows source ServiceReport link, BusinessDocumentItems, pricing evidence, approval-required warnings, editable internal line-correction forms, status history, command status, and explicit Internal Draft / Not sent / No Maven action / No inventory deduction states; line-resolution POST updates only quantity, unit price, pricing evidence, and approval-required flag with audit log; approval POST requires exact `APPROVE BUSINESS DOCUMENT` phrase and blocks pricing/quantity issues unless explicit override is checked; return-to-review POST requires a reason; Maven command POST remains separately gated by exact approval phrase and approved/ready status | Supabase PostgreSQL via Prisma `BusinessDocument` with linked items/logs/customer/service-report/AI-draft/Maven/AutomationCommand context when present; line-resolution POST writes only `BusinessDocumentItem` allowed fields and `BusinessDocumentLog`; approval/rejection POST writes only `BusinessDocument` status fields and `BusinessDocumentLog`; Maven command POST writes `AutomationCommand` only; no Maven/Invoice4U call, AutomationCommand execution, automatic AutomationCommand creation from approval, email/customer-facing action, inventory action, schema change, or source-system action | 0+ addressable business document rows after approved internal draft creation | `BusinessDocuments` detail/review, internal line resolution, internal approval workflow, and Maven draft command approval gate |
| `/business-documents/[id]/preview` | BusinessDocument Hebrew customer-facing HTML preview | Implemented read-only Hebrew RTL customer-facing document preview with company details, customer block, document metadata, Hebrew item table, Hebrew totals block, notes, footer/signature area, and compact A4 print layout; internal staging notes, evidence text, source/debug fields, and boundary warnings are not shown on the preview/PDF surface | Supabase PostgreSQL via Prisma `BusinessDocument` using the existing BusinessDocument adapter and internal engine output; GET only; no PDF generation by this page, Maven/Invoice4U call, email/customer-facing action, inventory action, DB write, schema change, or source-system action | 0+ addressable business document rows after approved internal draft creation | Internal BusinessDocument customer-facing preview before any approved external output |
| `/business-documents/[id]/pdf` | BusinessDocument temporary PDF download | Implemented internal PDF export MVP; GET loads the existing Hebrew `/business-documents/[id]/preview` route through Playwright/Chromium, prints A4 with print CSS and RTL layout, and streams `application/pdf` as an attachment with customer-facing filename | Supabase PostgreSQL via Prisma `BusinessDocument` through the existing adapter for document existence plus Playwright/Chromium rendering of the existing preview route; no DB write, no file persistence, no saved PDF record, no document sent/exported status mutation, no Maven/Invoice4U call, no email/customer-facing action, no inventory action, no schema change, and no source-system action | 0+ addressable business document rows after approved internal draft creation | Temporary document download for internal review before any approved external output |
| `/automation-commands` | Automation Commands list | Implemented queue review; shows command type, status, source BusinessDocument, source ServiceReport when linked, external target, dry-run result when present, and clear not-executed boundary before any Maven/Invoice4U/email/inventory action | Supabase PostgreSQL via Prisma `AutomationCommand` with linked `BusinessDocument` and `ServiceReport` context when present | 0+ addressable automation command rows after approved command creation | Automation command queue/list review |
| `/automation-commands/[id]` | Automation Command detail | Implemented command review plus protected Maven execution adapter dry-run for `CREATE_MAVEN_DRAFT`; GET shows status, lifecycle, source BusinessDocument, source ServiceReport, payload summary, raw source summary, dry-run blockers, would-send-to-Maven summary, idempotency/duplicate-protection evidence, and explicit Not externally executed / No Maven call / No email / No inventory boundaries; dry-run POST requires exact `DRY RUN MAVEN COMMAND` phrase and validates approved BusinessDocument, customer, document, line items, command payload, and idempotency | Supabase PostgreSQL via Prisma `AutomationCommand` with linked BusinessDocument and ServiceReport context when present; dry-run POST writes only internal `AutomationCommand.result`, `errorMessage`, `notes`, and `rawSource.mavenDryRun`; status remains safe and no Maven/Invoice4U call, external document creation, email/customer-facing action, inventory action, schema change, source-system action, or production action occurs | 0+ addressable automation command rows after approved command creation | Automation command detail/queue item review and Maven execution adapter dry-run |
